# Horas Grupo — Plan de construcción

Herramienta interna de imputación de horas para un grupo de empresas, con reporting FTE mensual y refacturación de servicios intragrupo. Este documento es el plan de trabajo para construirla con Claude Code.

---

## 1. Fuentes de verdad (no reinterpretar)

| Archivo | Qué define |
|---|---|
| `mocks/app_movil_empleado.html` | Empleado · móvil: pantallas, flujos (hoja por pasos), estados y diseño exactos |
| `mocks/empleado_web.html` | Empleado · escritorio: layout web-nativo (filas + calendario lateral), composer inline |
| `mocks/panel_administracion.html` | Admin · escritorio: sidebar, home KPIs, todas las secciones |
| `mocks/admin_movil.html` | Admin · móvil: drawer lateral, home día+mes, bandeja de ausencias |
| `supabase/migrations/001_esquema_inicial.sql` | Esquema base: empresas, proyectos, categorías, perfiles, tarifas, periodos, imputaciones, vistas de valoración y refacturación, RLS |
| `supabase/migrations/002_departamentos_ausencias_calendario.sql` | Departamentos, festivos y ajustes, ausencias con aprobación, faltantes y base FTE |

Los mockups son la especificación de UI/UX validada por el cliente: replicar 1:1 (layout, textos, estados, navegación). Los estilos ya están extraídos como tokens (sección 3).

**Matriz rol × dispositivo**: cada rol tiene DOS layouts sobre la misma lógica y los mismos componentes. No es responsive por estiramiento: en cada breakpoint (~980 px) se cambia la disposición según el mock correspondiente. Empleado: móvil = 3 pestañas + FAB + hojas inferiores por pasos; escritorio = sidebar plegable + composer inline con selects + modales centrados. Admin: escritorio = sidebar completo; móvil = drawer (hamburguesa) con las mismas 11 secciones agrupadas, home que fusiona día + resumen del mes, Ausencias funcional, y las secciones de gestión pura remiten al escritorio.

## 2. Stack y arquitectura

- **Supabase**: Postgres 15 + Auth (email/invitación) + RLS. Toda la lógica de negocio crítica vive en SQL (triggers, RPCs, vistas): el frontend nunca calcula tarifas ni valida topes.
- **Railway**: una app **Next.js 14+ (App Router, TypeScript, Tailwind)** que sirve las dos superficies:
  - `/` → app del empleado (mobile-first, tres pestañas + hoja de imputación)
  - `/admin` → panel (sidebar plegable, 11 secciones)
- Librerías: `@supabase/supabase-js` + `@supabase/ssr`, `exceljs` (exports), `date-fns` (con locale es), sin librería de gráficos (los quesos son `conic-gradient`, las barras son divs, como en los mocks).
- Emails de recordatorio: Resend + cron de Railway (fase 6).
- Variables de entorno: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (solo server), `RESEND_API_KEY`.

## 3. Sistema de diseño «Amable-gris»

Copiar los tokens tal cual a `globals.css` / config de Tailwind:

- Tipografías: **Nunito** (500–800) para todo; **IBM Plex Mono** para cifras y datos (`font-variant-numeric: tabular-nums`).
- Claro: fondo `#F6F6F4`, superficie `#FFFFFF`, sutil `#EFEFEC`, bordes `#E9E9E6`/`#D8D8D4`, tinta `#2E2E2B`/`#5C5C58`/`#8A8A85`/`#C4C4C0`, acción `#3A3A36` sobre blanco.
- Oscuro (conmutable, arranca según `prefers-color-scheme`): fondo `#141413`, superficie `#1D1D1B`, sutil `#262623`, tinta `#F0F0ED`…, acción invertida `#F0F0ED` sobre `#1B1B19`.
- Color SOLO en categorías: desarrollo `#5E7A8C`, diseño `#8C7A5E`, abogados `#7C6480`, gestión `#6E8B72` (variantes claras en modo oscuro, ver mocks).
- Estados sin color: punto relleno = completo/aprobado; punto hueco = parcial/pendiente; punto gris = futuro/inactivo; rechazado = tachado.
- Formas: tarjetas 16–18 px con sombra suave, botones píldora (primario oscuro), inputs rellenos sin borde, hoja inferior móvil 26 px, botón "añadir en vacío" cuadrado con borde discontinuo.

### 3.1 Patrones de implementación validados en los mocks
- **Delegación de eventos**: toda la interacción de listas re-renderizables pasa por atributos `data-act` y un manejador delegado (en React: handlers en el contenedor o por render declarativo, jamás bindings imperativos que se pierdan al repintar). Este patrón resolvió el bug de "Confirmar" del prototipo y es norma.
- **Estado de formularios sobrevive al re-render**: los selects/steppers del composer del precargado conservan su valor cuando el bloque se repinta (en React sale gratis con estado controlado; no usar `defaultValue` no controlado en estos casos).
- Quesos = `conic-gradient`; barras = divs; sin librería de gráficos. Cifras siempre en IBM Plex Mono con `tabular-nums` y formato es-ES (coma decimal).
- **Todo cambio en policies o funciones `security definer` re-ejecuta las verificaciones de alcance de los roles NO afectados por el cambio** (mínimo: un empleado normal, un admin_empresa, admin_grupo), con salida SQL. Un fix de RLS sin retest de regresión no cierra checkpoint.
- **Defensa en profundidad en hooks de "lo mío"**: cuando un hook expresa semánticamente "los datos del usuario logueado" (`useProyectosAsignados`, `useAusenciasMes`, `useImputacionesMes`, `usePerfil`, `useMaxHorasDia`...), su query lleva `.eq('empleado_id'|'perfil_id'|'id', user.id)` explícito — nunca delegar esa acotación solo en RLS. Motivo real, no hipotético: `ep_select` (empleado_proyecto) y `ausencia_select` dan a propósito lectura amplia a `admin_grupo`/`admin_empresa` sobre TODO el grupo/empresa (la necesitan pantallas admin legítimas: ficha de proyecto, bandeja de ausencias) — un hook de "lo mío" que confía solo en esa RLS filtra datos de otros empleados en cuanto lo usa alguien con rol admin viendo su propia vista de empleado. Bug real, encontrado dos veces en la misma sesión (`useProyectosAsignados` le mostró a un admin_empresa un proyecto ajeno; `useAusenciasMes` le mostró a un admin_empresa la ausencia de un compañero en "Mis ausencias"). Auditoría completa de `src/hooks/*.ts` (2026-09-08): `useDiasMes`/`useCategoriasTareas` consultan catálogos con `using(true)` uniforme para todos los roles (festivos, categorías) — no aplica, no son datos por-usuario. `useBalanceMes` llama a `balance_mes()`, `security definer` que resuelve `auth.uid()` **dentro** de la función (sin parámetro de empleado) — no puede filtrar mal porque no acepta a quién consultar. El resto (`useImputacionesMes`, `usePerfil`, `useMaxHorasDia`) ya llevaban el `.eq()` explícito antes de esta auditoría. Mismo criterio aplica a hooks admin que consultan datos de UN empleado concreto elegido en un formulario (`useAsignacionesEmpleado`): explícito por diseño, ahí la RLS amplia es exactamente lo que se está usando a propósito.

### 3.2 Divergencias deliberadas del mock (decisiones cerradas, no deuda)
- **Selector de rango de fechas en ausencias**: dos toques explícitos (primero = inicio, segundo = fin, tercero reinicia), nunca colapsar una selección multi-día suelta a min→max. El mock no distingue el gesto; se fija esta interacción como norma para evitar ambigüedad sobre qué días quedan seleccionados.
- **Campo "Cliente" en alta de proyecto** (`panel_administracion.html`, sección Proyectos): se omite. La tabla `proyecto` no tiene esa columna y no se añade — el "cliente" de un proyecto en este modelo YA es su empresa del grupo (`proyecto.empresa_id`, la destinataria de horas y de refacturación); un campo de texto libre duplicaría ese dato y podría contradecirlo. Si en el futuro hace falta un cliente externo al grupo, es un modelo de datos distinto (tabla `cliente`), no un texto suelto.
- **Tarifas: identidad del empleado en tarifas personales**. `tarifa_select` es visible para ambos roles admin por diseño (todo el grupo); pero cuando la tarifa es personal (`empleado_id` no nulo) y ese perfil no es visible para quien consulta (fuera de su ámbito de `perfil_select`), el `join` devuelve `null`. Decisión: no ensanchar `perfil_select` por este motivo (sería relajar RLS por algo cosmético, justo lo contrario de 007/008), ni mostrar iniciales (sigue exponiendo identidad parcial sin necesidad). Se resuelve en presentación: la celda muestra el placeholder explícito "Empleado de otra empresa" (`ink-tertiary`, `title="Tarifa personal fuera de tu ámbito"`) en vez de quedar en blanco — el dato que sí concierne al admin (existe una tarifa personal, a qué precio, con qué vigencia) queda íntegro, y la ocultación se lee como intencional, no como un bug.

## 4. Reglas de negocio (resumen ejecutable)

1. **Imputación diaria**: el empleado imputa horas por día sobre subcategorías (tareas) de proyectos a los que está asignado (`empleado_proyecto`). Empresa y categoría van implícitas (proyecto → empresa destino; subcategoría → categoría). Jornada 7 h/día laborable; tope duro 12 h/día (trigger).
2. **Estados**: `borrador → enviada → aprobada|rechazada → cerrada`. Aprueban admin de la empresa destino o admin de grupo. El cierre de periodo (mes) congela todo.
   **Qué estados cuentan según el consumidor** (no reabrir sin motivo — documentado también como comentario en `005_fte_mes_solo_confirmadas.sql`): `balance_mes()` (saldo propio del empleado, mes en curso) suma `estado <> 'rechazada'` — todo lo suyo salvo lo explícitamente rechazado, porque el empleado necesita ver su propio progreso aunque aún no esté aprobado. `fte_mes()` (reporting/FTE de admin, base del Excel oficial) suma **solo** `estado in ('aprobada','cerrada')` — un borrador o una imputación enviada todavía no está confirmada, no debe figurar en el informe de horas del grupo.
3. **Requeridas**: laborables del mes × jornada, descontando festivos (grupo o empresa) y días con ausencia aprobada.
4. **Ausencias**: solicitud desde la app (vacaciones/baja/permiso) → aprueba responsable de departamento o admin. Aprobada = bloquea imputación esos días y descuenta requeridas.
5. **Tarifas**: `resolver_tarifa()` — prioridad empleado > categoría; filtro opcional por empresa origen; vigencias por fecha. Horas sin tarifa bloquean el cierre.
6. **Refacturación**: solo si `empresa_origen (del empleado) ≠ empresa_destino (del proyecto)` y el proyecto es refacturable. Vista `v_refacturacion_mensual`.
7. **Excel FTE mensual** (columnas y fórmulas cerradas en F4, sustituyen la redacción original de este punto): 10 columnas — Empleado · Horas Imputadas · Horas Requeridas · Vacaciones (días) · Bajas (días) · **Permisos (días)** · Proyecto · Horas Proyecto · FTE s/imputadas · FTE s/requeridas (11 si se exporta el grupo completo: se antepone "Empresa"). `FTE s/imputadas = Horas Proyecto / Horas Imputadas del empleado` (las filas de una persona suman 1,00, reparto del trabajo real). `FTE s/requeridas = Horas Proyecto / requeridas_efectivas del empleado` (neto de ausencias, único helper en `src/lib/horas/requeridas-efectivas.ts`; las filas de una persona suman su ratio imputadas/requeridas, puede superar 1,00; celda vacía si requeridas_efectivas ≤ 0). La suma de "FTE s/requeridas" de todas las personas de un proyecto es el FTE que ese proyecto consumió en el mes — la cifra que justifica la refacturación. **Se abandona el mecanismo "líneas (ausencia)"** de la redacción original: las tres columnas de días ya representan la ausencia sin inventar un proyecto ficticio. Base: `fte_mes(anio, mes)` + `?empresa=<id>` opcional para acotar el archivo (sin él, grupo completo con columna Empresa; con él, esa empresa sin columna Empresa, ambigua si se mezclase con otras). Endpoints: `src/app/api/export/fte/route.ts`, `src/app/api/export/refacturacion/route.ts`.

## 5. Fases

### F1 · Cimientos (medio día)
- Repo Next.js + Tailwind con tokens; layout raíz con tema claro/oscuro.
- Proyecto Supabase; aplicar migraciones 001 y 002; `supabase/seed.sql` (sección 6).
- Auth por invitación (email + magic link), middleware de sesión, hook `usePerfil()`.
- **Hecho cuando**: login funciona, un empleado ve solo sus proyectos asignados vía RLS (probar con dos usuarios).

### F2 · App del empleado (2–3 días)
Replicar `app_movil_empleado.html` (móvil) y `empleado_web.html` (escritorio) con componentes compartidos:
- Inicio: tarjeta de hoy + KPIs mes/requeridas/balance, carrusel "Últimos días imputados" con **Reutilizar** (precarga staged), calendario del mes.
- Imputar: navegación de días, bloque **Precargado** (steppers ±0,5 h por línea, "＋ Imputar nueva tarea" al lote, Confirmar/Descartar → inserta como `borrador`), cuadro del día con CTA punteado, "Últimas imputaciones" agrupadas por día con "Usar", popup "Imputaciones anteriores".
- Hoja de nueva imputación: Proyecto|Ausencia → proyecto → tarea (agrupada por categoría con cat-dot) → horas (stepper, atajos, multi-día) / tipo de ausencia → días → `solicitar_ausencia()`.
- Calendario: mes con estados + lista de ausencias propias.

**Layout web (escritorio, `empleado_web.html`)** — mismo estado y lógica, otra disposición:
- Sidebar plegable (chevron → 64 px solo iconos) con Inicio / Imputar / Calendario y pie con tema + enlace al panel admin.
- **Imputar = filas apiladas + calendario lateral**: columna principal con 3 filas (1· tira de fecha: flechas + total + barra + estado; 2· imputación del día: tabla editable proyecto·tarea / empresa / horas ± / eliminar, con **composer inline al pie** — select de proyecto con su empresa, select de tarea con `optgroup` por categoría, stepper, botón Añadir; 3· anteriores imputaciones) y columna derecha de 400 px con el **calendario del mes grande y sticky**, operativo: clic en día laborable → lo carga en el editor.
- **Precargado (staged)**: líneas con stepper ±0,5 h, **composer propio "＋ Añadir al lote"** (misma fila de selects) y Confirmar (total en vivo) / Descartar. "Reutilizar día" desde Inicio precarga sobre **hoy**; desde Imputar, sobre el **día seleccionado**.
- **Módulo `FilaDia` compartido** entre Inicio ("Últimos días imputados") e Imputar ("Anteriores imputaciones"): fecha+total | líneas con horas | botón "Reutilizar día". La repetición de líneas sueltas ("Usar") vive solo en el modal "Ver todo".
- Modales **centrados** (no hojas inferiores) únicamente para: histórico completo y solicitud de ausencia (formulario directo: tipo + desde/hasta).
- En móvil (<~980 px o user-agent móvil) se sirve el layout del mock móvil: pestañas + FAB + hojas por pasos.
- **Hecho cuando**: el flujo completo de AMBOS mocks funciona contra Supabase real, incluido reutilizar+ajustar+añadir al lote+confirmar y solicitar vacaciones, en los dos layouts.

### F3 · Panel de administración (2–3 días)
Replicar `panel_administracion.html`:
- Sidebar plegable con grupos e iconos; home con día navegable (KPIs + pendientes + ausentes), mes navegable (KPIs + calendario sincronizado + quesos + refacturación), botones directos.
- Secciones: Usuarios (invitar vía service role), Ausencias (bandeja aprobar/rechazar → RPCs), Empresas, Proyectos (con **ficha de proyecto**: queso por categoría, departamentos, personas), Categorías, Calendario (jornada, festivos, requeridas/mes), Control (`faltantes()` + imputación directa), Tarifas, Refacturaciones (detalle + cierre), Ajustes (tabla `ajuste`).
- **Layout móvil (`admin_movil.html`)**: sin tabbar — **drawer lateral** (hamburguesa en cabecera) con las 11 secciones agrupadas (General/Personas/Estructura/Operación/Sistema), activo con franja de 3 px. Home = bloque del día (KPIs 2×2 + pendientes con Recordar + ausentes) seguido del **Resumen del mes completo** (navegador ‹›, KPIs, barras por empresa, queso de proyectos en grises, refacturación con total). Ausencias funcional (aprobar/rechazar). Las secciones de gestión (Usuarios, Empresas, Proyectos, Categorías, Calendario, Control, Tarifas, Refacturaciones, Ajustes) muestran pantalla de remisión al escritorio: en móvil el admin vigila y aprueba, no configura.
- **Hecho cuando**: cada card de ambos mocks muestra datos reales y las acciones (aprobar ausencia, crear tarifa, festivo) persisten.

### F3.5 · Despliegue de demo (Railway) — hecho antes de F4

Objetivo: app accesible desde un móvil real y enseñable, contra el Supabase real (Timerx), sin tocar el dataset de seed.

**URL pública**: `https://timerx-production.up.railway.app`

- **Proyecto Railway**: ya existía un proyecto `timerx` (servicio `timerx`, entorno `production`) conectado a `olipp7warlands/timerx` rama `main` con autodeploy — no se creó nada nuevo, solo se enlazó el repo local (`railway link`) y se completó lo que faltaba: variables, dominio, cabeceras. Build con Railpack (autodetecta Node/Next.js vía `railpackInfo` — sin Dockerfile ni `railway.toml`; no hizo falta tocar puerto/host, Next.js escucha en `$PORT` automáticamente bajo Railpack).
- **Variables** (`railway variable set`): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`. Ninguna es "solo servidor" a nivel Railway — esa distinción la hace Next.js en build: solo `NEXT_PUBLIC_*` se inlinea en los chunks de cliente. Verificado con `grep` del valor literal de `SUPABASE_SERVICE_ROLE_KEY` sobre `.next/static` tras un build local con las mismas variables → **cero coincidencias en todo `.next`, ni siquiera en chunks de servidor** (Next.js lee `process.env` en runtime, nunca inlinea vars sin el prefijo). Como control de que el método de búsqueda era válido, el mismo `grep` con el valor de `NEXT_PUBLIC_SUPABASE_ANON_KEY` sí aparece en un chunk de cliente, como corresponde.
- **Dominio**: generado con `railway domain` (no existía ninguno todavía). Tras fijar las variables `NEXT_PUBLIC_*` hizo falta un **redeploy** (`railway redeploy`) porque esas variables se compilan en build time, no en runtime — el deployment ya "SUCCESS" en el repo no las tenía.
- **Supabase Auth**: `site_url` cambiado a la URL de Railway (antes `http://localhost:3000`) y `additional_redirect_urls` con los puertos locales (`3000`, `3002`, `127.0.0.1:3000`) para que el desarrollo local siga funcionando con `redirectTo` explícito. Aplicado vía `supabase config pull` + `supabase config push`, con un `supabase/config.toml` **deliberadamente mínimo** (solo declara `project_id` y `[auth].site_url`/`additional_redirect_urls`): un primer `config pull` completo trajo toda la configuración remota (storage, realtime, pooler, mfa, sms...) y el diff previo a pushear reveló que habría **desactivado Twilio SMS** (activo en remoto) como efecto colateral de plantillas por defecto de `supabase init` — se recortó el archivo a solo lo que esta tarea necesita gestionar, así ningún push futuro puede tocar configuración ajena sin declararla explícitamente primero.
- **Cabecera `X-Powered-By: Next.js`**: detectada al auditar cabeceras del despliegue real, eliminada con `poweredByHeader: false` en `next.config.ts`. Resto de cabeceras revisadas en `/`, `/admin`, `/login` — nada más que exponer.
- **Verificaciones contra el despliegue real** (no local): `/debug`, `/debug/movil`, `/debug/movil-admin` → 404 (`curl` directo al dominio público); magic link de Andrés generado con `redirectTo` al dominio de Railway, sesión establecida y home real cargando datos reales; intento de imputar en un día ya al tope (12 h) rechazado por el trigger sin insertar fila — confirmado por SQL antes/después, dataset de seed intacto; `/admin` gated en el dominio real para Andrés (→ `/`) y Cristian (panel completo).
- **No se tocaron datos**: ninguna de las pruebas anteriores dejó una imputación, ausencia ni fila nueva en el seed — es el escenario que se enseña.

### F4 · Cierre y exports (1 día) — hecho

- `cerrar_periodo()` ya estaba conectada de extremo a extremo desde F3 (RPC → `useRefacturacion` → botón en `RefacturacionEscritorio.tsx`); F4 cerró 3 huecos reales: sus dos precondiciones (pendientes, horas sin tarifa) pasan de excepciones secuenciales a un único mensaje combinado cuando coexisten (migración 011); el KPI "Estado del periodo" lee el estado real de `periodo` en vez de estar hardcodeado a "Abierto"; y ahora hay selector de mes a cerrar (antes solo el mes en curso).
- **`estado_dias_mes(anio, mes)`** (migración 011): agregado por-día-del-mes que faltaba para el calendario "Seguimiento del día" del admin (`InicioEscritorio.tsx`) — antes usaba una leyenda falsa ("Seleccionado/Laborable/Futuro") derivada solo del día seleccionado, sin datos reales. El requerido de cada día descuenta ausencias aprobadas por empleado y respeta el festivo de cada empleado por su propia empresa; verificado contra el caso real del seed (baja aprobada de Andrés, 1-2 sept).
- Exports `exceljs` (FTE mensual y refacturación) — ver sección 4 punto 7 para las columnas y fórmulas definitivas del FTE. Endpoints server-side (`src/app/api/export/{fte,refacturacion}/route.ts`), sesión del propio admin (RLS respetada, sin service role).
- Verificación de `cerrar_periodo()` en su camino de éxito (muta de verdad) hecha contra una empresa+empleado+proyecto+tarifa+imputación **sintéticos**, creados y borrados en la misma sesión (incluida la fila de `auth.users`) — no se tocó ninguna fila real de Wowinx/Málaga CF SAD/Legal Norte; confirmado por SQL que julio/agosto siguen cerrados y septiembre sigue abierto en las 3 empresas reales tras la prueba.
- **Decisión aceptada — vulnerabilidad transitiva de `exceljs`**: `npm audit` reporta una moderada en `uuid` (`GHSA-w5hq-g745-h8pq`, falta de comprobación de límites de buffer en `uuid.v3/v5/v6` cuando se le pasa un buffer) arrastrada por `exceljs`. Aceptada tal cual, no se aplica el fix — razones: (1) es transitiva, no código propio; (2) `exceljs` la usa internamente para IDs de objetos del `.xlsx` que genera, sin que ningún dato de entrada externa (parámetros de usuario, contenido de la BD) llegue nunca a esas llamadas a `uuid` — no hay vector de explotación real en cómo se usa aquí; (3) el único fix vía `npm audit fix --force` baja `exceljs` a `3.4.0`, un downgrade con cambios incompatibles, un coste desproporcionado para un riesgo sin vector real. Revisar si `exceljs` publica una versión que actualice `uuid` sin downgrade.

### F5 · Aprobación de imputaciones (1 día) — hecho

Sin mock que replicar (ni el empleado ni el admin lo cubren en los HTML de referencia) y sin migración nueva — `enviar_imputaciones()`, `aprobar_imputaciones()`, `rechazar_imputaciones()` ya existían completas desde la 001, nunca conectadas a ningún UI.

- **Envío**: botón único "Enviar mes" (sin checkboxes) en Imputar (ambos layouts), visible solo si hay líneas `borrador`/`rechazada` en el mes, con contador y modal/hoja de confirmación previa (resume líneas+horas, avisa de que dejan de ser editables). `enviarPendientes()` en `useImputacionesMes.ts`.
- **Rechazo**: motivo obligatorio en UI (botón deshabilitado si vacío) — **no hay `check` a nivel de base de datos**, `rechazar_imputaciones` acepta motivo vacío/null sin protestar; es una limitación conocida y aceptada, no una garantía de la BD. Motivo visible al empleado bajo la línea rechazada (micro-texto atenuado, sin tachar — la línea sigue editable/reenviable).
- **Aprobación**: solo `admin_grupo`/`admin_empresa`, acotado por la **empresa destino del proyecto** (`empresa_de_proyecto(proyecto_id) = auth_empresa()`, confirmado leyendo el código — no la empresa de origen del empleado). `proyecto_responsable` sigue siendo de solo lectura: **aprobación por responsable de proyecto descartada conscientemente en F5**, no se ha tocado `aprobar_imputaciones`/`rechazar_imputaciones` para no inestabilizar RPCs ya probadas; revisable si se pide explícitamente.
- **Bandeja admin**: nueva card "Aprobación de imputaciones" dentro de **Control** (`ControlEscritorio.tsx`), no como sección nueva del sidebar — `secciones.ts` documenta sus 11 secciones como réplica exacta del mock, que no tiene una 12ª sección para esto. Al no estar Control en `SECCIONES_MOVIL_FUNCIONALES`, hereda gratis la pantalla de remisión a escritorio ya existente en móvil, sin trabajo nuevo.
- ~~Candidata F6: bandeja de aprobación de imputaciones en admin móvil~~ **Decidido en el Lote 4: NO entra en admin móvil en v1** (ver "Decisión de alcance cerrada" en el Lote 4).
- **Seed** (script, no a mano): 3 líneas `enviada` añadidas a `supabase/seed_datos.sql` §8 para que la bandeja se enseñe poblada desde el primer vistazo — Leo Silva/Web corporativa (destino Málaga CF SAD) los días 8 y 9 de sept, Sara Martín/Interno (destino Wowinx) el día 4. Elegidas por tener hueco real bajo el tope de 12h/día y no tocar los cuadres FTE de F4 (Andrés, Ximeras) — `fte_mes()` de todas formas ignora `enviada` por completo (`estado in ('aprobada','cerrada')`), así que esto no podría moverlos aunque quisiera.
- **Verificado**: ciclo SQL completo borrador→enviada→rechazada(con motivo)→editada→reenviada→aprobada (motivo limpiado por el propio RPC); ámbito destino confirmado con Marina (admin_empresa Málaga CF SAD) — su bandeja solo muestra las 2 líneas de Leo, no la de Sara, y aprobó una de verdad; ciclo UI completo en ambos layouts (envío con confirmación, rechazo con motivo inline en la bandeja, motivo visible de vuelta en Imputar). Datos de prueba revertidos, seed final con las 3 líneas en `enviada` intactas.

### F6 Bloque A · Recordatorios + descripción obligatoria real — hecho

- **`faltantes_recordatorio()`** (012): gemela de `faltantes()` sin el gate de `auth_rol()` — ese gate da 0 filas siempre si se invoca sin sesión de usuario, lo que habría hecho inútil un cron llamando a `faltantes()` directamente. Revocada a `authenticated`/`anon`, solo `service_role`.
- **Disparadores**: cron diario (`src/app/api/cron/recordatorios/route.ts`, protegido con `Authorization: Bearer CRON_SECRET`, respeta `ajuste.recordatorio_email`) + botón manual "Recordar"/"Recordar por email" en Inicio y Control (siempre disponible, misma lógica vía `enviarRecordatoriosManual()` en `src/app/admin/actions.ts`, pero leyendo `faltantes()` con la sesión real del admin para heredar su ámbito exacto). Módulo compartido `src/lib/recordatorios/enviar.ts`.
- **Destinatarios**: un email por empleado con sus propios días faltantes, ventana de 5 días, sin digest al admin, guarda anti-spam real (`perfil.ultimo_recordatorio_en`, máx. 1 por empleado y día natural, aplica también en modo `log`).
- **`MODO_EMAIL`**: no hay cuenta Resend confirmada todavía — por defecto `'log'` (registra en `recordatorio_log` sin llamar a Resend; verificable sin depender de una bandeja de email), `'real'` solo si la variable de entorno lo dice explícitamente (`npm install resend`, import perezoso, no falla sin `RESEND_API_KEY` en modo log).
- **Infraestructura Railway**: `CRON_SECRET` en el servicio web; segundo servicio `timerx-cron-recordatorios` (imagen `curlimages/curl`, sin build) con `cronSchedule` diario (`serviceInstanceUpdate` vía `railway api`, el CLI no lo expone como flag) cuyo `startCommand` hace `curl` a la ruta protegida — el `cronSchedule` nativo de Railway es para servicios que arrancan y terminan, no para el servicio web siempre-encendido.
- **`descripcion_obligatoria()` real** (012, enforcement solo en `INSERT` — toda imputación existente, seed incluido, tiene `descripcion` `null`; aplicar el check también a `UPDATE` habría roto ajustar horas/enviar/aprobar/rechazar/cerrar sobre cualquier línea legada). UI en los 3 puntos de entrada (composer de escritorio, wizard móvil, imputación directa admin) + precargado editable línea a línea (`actualizarDescripcionStaged`) + guardas en `usarLinea()`/`confirmarStaged()` para los caminos de copia, que de otro modo intentarían insertar una `descripcion` vacía heredada de líneas legadas.
- **Valor del ajuste en la demo**: verificado con `descripcion_obligatoria = true`, dejado en **`false`** al terminar (el seed entero carece de descripciones; el toggle, ya editable, permite enseñarlo en vivo sin forzarlo). El runbook de producción decide su propio valor (recomendado `true` desde el día 1, sin legado que romper).
- **`bloquear_meses_cerrados`**: toggle retirado de `AjustesEscritorio.tsx` — el bloqueo ya era incondicional en el trigger, el flag no hacía nada; la columna en `ajuste` se queda sin uso, documentada aquí.
- Runbook de despliegue a producción real: `docs/runbook-produccion.md`, con la decisión de proyecto Supabase nuevo ya confirmada.

### F6 Bloque B · Mapa del grupo — hecho
- Migración 013 (`mapa_area`, `mapa_item`): lectura para todo `authenticated`, escritura solo `admin_grupo` (mismo patrón que `ajuste`/`categoria`). "Eliminar" en la UI es baja lógica (`activa`/`activo = false`, nunca `delete`) — un área con elementos activos no puede eliminarse. 6 áreas / 22 elementos sembrados literales del mock en `supabase/seed.sql` §6.
- 4 superficies de consulta compartiendo `useMapa()` + `MapaGrid.tsx` (mismo acordeón, sin "cerrar los demás"): modal en escritorio empleado, hoja inferior en móvil empleado, pantalla real en admin móvil (grupo Estructura, con nota de que la edición vive en escritorio), y vista previa en vivo dentro de la gestión de escritorio.
- Gestión (`MapaEscritorio.tsx`, sidebar Estructura tras Calendario): alta/edición/reordenación (`↑`/`↓`, intercambia `orden` con el vecino) y baja lógica con confirmación (`src/components/ui/confirmar.ts`, hoy `window.confirm`, sustituible en un solo sitio) para áreas y elementos. Paleta fija de 8 tonos (`src/lib/mapa/paleta.ts`) — nunca un color-picker libre.
- **Verificado**: build limpio; RLS real (Andrés y Marina leen y no escriben, rechazo confirmado por policy; Cristian CRUD completo); ciclo dinámico real (área + elemento creados por Cristian vía UI, visibles para Andrés sin redeploy, luego eliminados y confirmados como baja lógica en BD, no como fila borrada); render contrastado contra los 4 mocks vía `/debug/movil` y `/debug/movil-admin`.

### F6 Bloque B · Menú de usuario/logout + espaciado de formularios — hecho
- Primera forma de cerrar sesión en toda la app: `src/components/ui/MenuUsuario.tsx` (`MenuUsuarioDesktop`/`MenuUsuarioMovil`, compartido por las 4 shells) — popover anclado (se abre hacia arriba, el trigger vive al pie del sidebar) en escritorio, hoja inferior (reutiliza `BottomSheet`) en móvil. Cabecera no clicable con nombre/email/rol (`ETIQUETA_ROL` centralizada en `src/lib/auth/roles.ts`) + "Cerrar sesión" con navegación dura (`window.location.assign('/login')`, no `router.push` — evita el bug clásico de estado del usuario anterior sobreviviendo en memoria). `email`/`rol` enhebrados hasta `EmpleadoCtx`/`AdminInfo` desde `getPerfilServer()`, que ya los traía.
- Fix sistémico de espaciado: `.card-body > .btn { margin-top: 16px }` en `globals.css` (selector de hijo directo, no toca botones ya envueltos en su propio contenedor) — corrige de una vez todos los formularios en card de admin y empleado sin tocar componente a componente.
- **Verificado**: build limpio; popover cierra con click fuera y `Escape`; ciclo logout completo contra producción (Marina cierra sesión → `/login` → login Andrés → sus propios datos 172/140/+32, sin restos; repetido en escritorio admin con Cristian).

### F6 Bloque B · Fichas operativas (usuario, proyecto ampliada, filas clicables) — hecho
- Migración 014 (aplicada en el paso anterior): `balance_mes_empleado()` + helper `dias_ausencia_en_mes()` que corrige el recorte por mes de `balance_mes()`/`fte_mes()` (ver entrada de esa migración más arriba).
- **Ficha de usuario** (`FichaUsuarioEscritorio.tsx`, nueva — list↔detail desde `UsuariosEscritorio.tsx`, mismo patrón que la ficha de proyecto): KPIs (balance del mes, requeridas efectivas, días de ausencia, pendientes), reparto por proyecto en barras, proyectos asignados con asignar/finalizar (`useAsignaciones.ts`, compartido con la ficha de proyecto — una sola implementación de la mutación sobre `empleado_proyecto`), sus enviadas pendientes (`TablaPendientesImputacion.tsx`, extraída de `ControlEscritorio.tsx` para no duplicar el flujo de rechazo-con-motivo), sus ausencias del mes con aprobar/rechazar, últimas imputaciones, y acciones (recordar por email de un solo empleado, imputación directa con hand-off a Control vía empleado preseleccionado, editar datos con confirmación en cambios de empresa/rol, desactivar). `UsuariosEscritorio.tsx` posee la única instancia de `useUsuarios()`; la ficha recibe `usuario` derivado en vivo por id y los mutadores como props — evita el bug de estado obsoleto tras guardar una edición.
- **Ámbito intragrupo**: `admin_empresa` ve la ficha de un intragrupo (herencia de `perfil_select`) pero con aviso "Fuera de tu empresa" en vez de KPIs (`balance_mes_empleado` usa el mismo `admin_puede_ver_empresa` estricto sin excepción intragrupo); "Editar datos"/"Desactivar" ocultos (no solo deshabilitados). Verificado además que `aprobar_ausencia`/`rechazar_ausencia` son estrictos por empresa sin excepción intragrupo (`puede_resolver_ausencia`, 002) — y que la propia `ausencia_select` ya es estricta por empresa, así que un intragrupo ni siquiera aparece en la sección "Ausencias" de un admin_empresa; los botones de aprobar/rechazar se ocultan igualmente por defensividad.
- **Ficha de proyecto ampliada** (`FichaProyectoEscritorio.tsx`): columna "Acciones" con "Finalizar" por persona y, al pie, "＋ Añadir persona" con selector filtrado a empleados sin asignación vigente a ese proyecto (`useAsignacionesProyecto`) — al añadir, aparece al instante como "recién asignada · 0,0 h" (optimista, igual que el mock, sin esperar imputaciones).
- **Filas clicables**: `UsuariosEscritorio.tsx` y `ProyectosEscritorio.tsx` — fila entera abre la ficha (guard `closest('button')` para no disparar dos veces con el botón "Ver"), regla `.row-link` añadida a `globals.css` (cursor + hover, literal del mock).
- Espaciado `.card-body > .btn` corregido a la regla real de dos niveles del mock: 14px general, 16px cuando el botón sigue directamente a un `select`/`input` (Frente 2 había puesto 16px uniforme).
- **Verificado**: build limpio; ficha de Andrés cuadra exacto contra lo ya conocido (172/140, +32, reparto 68/64/40) tanto local como en producción; ciclo de asignación en los dos extremos (Cristian asigna Leo→Triatix desde la ficha de proyecto, aparece en su ficha y en el composer del propio Leo el mismo día — `hasta=hoy` sigue siendo imputable hoy, desaparece del selector al día siguiente — luego Finalizar y revertido/limpiado tras la prueba); aprobación de la línea Interno de Sara desde su ficha y reversión a `enviada` (con un desliz corregido en el propio proceso: había dos líneas el mismo día, se restauró la que no correspondía antes de continuar); logout multi-identidad (Frente 1, sin regresión); RLS con las 3 identidades (Cristian completo, Marina con aviso intragrupo y acciones ocultas, Andrés redirigido fuera de `/admin`); capturas contra el mock; commit + push + Railway (`SUCCESS`) + reverificación de fila clicable, ficha de usuario y ficha de proyecto contra producción.

### F6 Bloque B · Empresas al patrón listado→ficha + hand-off genérico — hecho
- Empresas gana el mismo patrón listado→ficha de Usuarios/Proyectos: tabla (Empresa/CIF/Proyectos/Horas·mes/Refacturación·mes/Estado/Acciones) con fila `.row-link` + "Ver"; ficha (`FichaEmpresaEscritorio.tsx`, nueva) con 4 KPIs, "Proyectos de la empresa" (con "＋ Nuevo proyecto" inline — nombre + código, el código es una desviación necesaria del mock: `proyecto.codigo` es `not null unique(empresa_id,codigo)` en el esquema real), "Empleados" (con "＋ Invitar usuario ›"), "Refacturación del mes" y "Acciones sobre la empresa" (Editar datos/Desactivar).
- `useEmpresas.ts` gana `actualizar`/`desactivar`. `EmpresasEscritorio.tsx` lifta `useEmpresas`/`useProyectosAdmin`/`useUsuarios` (instancia única, se pasan a la ficha como props ya filtradas + sus mutadores) — mismo criterio anti-staleness que la ficha de usuario del Frente 3: crear un proyecto desde la ficha actualiza también el contador "Proyectos activos" del listado al volver, sin remount.
- **Hand-off genérico** en `ShellEscritorioAdmin.tsx` (`handoff` + `irA()` + `handoffPara()`), sustituye el estado puntual que antes solo servía para Control: ahora cubre también abrir una ficha de Proyectos/Usuarios desde la ficha de empresa (navegación cruzada proyecto↔ficha, empleado↔ficha) y precargar el formulario de invitación con una empresa. Bug real corregido en el propio diseño: el hand-off de `ProyectosEscritorio` solo se consume dentro del `if (encontrado)`, nunca incondicional — si no, la primera vez que `proyectos` sigue cargando al cambiar de sección, la navegación cruzada perdería la preselección y aterrizaría en el listado.
- Guard de "Desactivar empresa" cuenta solo proyectos y empleados **activos** (un perfil ya desactivado no bloquea para siempre la baja de su antigua empresa) — verificado en vivo alternando `perfil.activo` de un empleado real y confirmando que el conteo lo excluye. **Hallazgo documentado**: a diferencia de `perfil.activo` (que sí filtra `faltantes()`/`resumen_dia()`/etc.), `empresa.activa` no tiene HOY ningún efecto downstream real — ninguna policy RLS, vista ni selector en todo el código lo consulta (confirmado por grep en `supabase/migrations/` y `src/`); es puramente una etiqueta visual en el listado/ficha.
- Ámbito: listado y fichas visibles/clicables para todos (`empresa_select`/`proyecto_select` son abiertos por diseño ya existente, no una fuga nueva) — para una empresa ajena, KPIs de horas/refacturación simplemente salen a 0 si no hay solape con los propios proyectos/empleados del admin_empresa (RLS de `imputacion`/`v_refacturacion_mensual`, sin gate adicional, verificado con Marina viendo Wowinx SL). Editar/Desactivar ocultos para admin_empresa en TODAS las fichas, incluida la propia (`empresa_admin` es RLS estricta, sin excepción de empresa propia — a diferencia de usuario). Atajos "＋ Nuevo proyecto"/"＋ Invitar usuario" solo si `admin_grupo` o la ficha es la empresa propia (coincide con `proyecto_admin`/`perfil_insert_admin`).
- Categorías reordenada tras Mapa en el sidebar (Estructura): Empresas, Proyectos, Calendario, Mapa, Categorías.
- **Verificado**: build limpio; cifras del listado exactas en local y producción (Wowinx 554,5 h/4.620,00 €, Málaga 272,0 h/15.860,00 €, Legal Norte 182,0 h/1.440,00 €); ciclo crear proyecto desde la ficha de Málaga → aparece en Proyectos y en la propia ficha sin recargar → limpiado; hand-off de invitación con empresa preseleccionada; navegación cruzada en ambos sentidos (incluido un proyecto sin actividad, para probar la rama de estado vacío); ámbitos con las 3 identidades; commit + push + Railway (`SUCCESS`) + reverificación contra producción.

### F6 Bloque B · "Computar día" (empleado) + emails demo a @wowinx.com — hecho
- Cambio de semántica de producto (sin mock, spec de texto): la acción principal del empleado pasa de "enviar el mes" a "computar el día". Mismo RPC `enviar_imputaciones(p_ids uuid[])` (001, sin cambios — ya era genérico, sin columna de rango de fechas), acotado por los ids que le pasa el caller. `useImputacionesMes.ts` gana `computarDia(fecha)` junto al ya existente `computarTodo()` (renombrado de `enviarPendientes`, mismo alcance: todo el mes cargado).
- Botón "Computar día" en 4 superficies (Inicio y Imputar, escritorio y móvil) vía hook compartido `useComputarDia` (`src/components/empleado/compartido/useComputarDia.ts`) — misma lógica, cada capa con su propio shell de confirmación (`ModalCentrado` en escritorio, `BottomSheet` en móvil). La barra de pendientes deja de ser el disparador principal: pasa a informativa + escoba ("N líneas por computar en M días" + botón secundario "Computar todo").
- **Vocabulario, no esquema**: barrido completo de "enviar/Enviar/envío" en las 4 superficies (`ModalAusencia.tsx`'s `enviar()` no se toca — dominio distinto, solicitud de ausencia). `ESTADO_ETIQUETA` (dos definiciones, ambas solo del lado empleado, sin compartir con admin) cambia `enviada: 'Enviada'` → `enviada: 'Computada'` — es capa de presentación, no el enum de BD, que sigue igual.
- **Verificado sin efectos colaterales**: `cerrar_periodo` sigue bloqueando igual con `borrador`/`enviada` mezcladas venidas de "computar día"; `faltantes()`/`faltantes_recordatorio()` no miran si el envío fue por día o por mes (solo filtran `estado <> 'rechazada'`); la bandeja de Control (`useAprobacionImputaciones`, filtro `estado = 'enviada'` sin cláusula de fecha) recibe un lote de día exactamente igual que uno de mes.
- **11 emails de demo `@sirtana.net` → `@wowinx.com`** (`scripts/cambiar-emails.mjs`, nuevo): formato `nombre.apellido@wowinx.com`, cambiados vía `auth.admin.updateUserById` + `perfil.email` explícito (confirmado que no existe trigger de sincronización para `UPDATE`, solo el `on_auth_user_created` que es `AFTER INSERT`). Preflight que aborta sin mutar nada si el nombre real en BD no corresponde al local-part del email nuevo (protege contra un id traspuesto en el array); verificación final por correspondencia nombre↔email releída, no por conteo de updates OK. `scripts/seed-usuarios.mjs` actualizado al mismo dominio para que un re-seed futuro nazca ya consistente. `olcasan08@gmail.com` (acceso real del usuario) no está en ningún array de estos scripts — no requirió exclusión, ya estaba fuera por construcción.
- **Verificado**: build limpio; capturas del flujo computar (día con líneas → botón → computado; barra con la escoba); regresión de aprobación con un lote de día; login real con un email `@wowinx.com` nuevo contra local y producción; tabla de faltantes muestra los emails nuevos; commit + push + Railway (`SUCCESS`) + reverificación en producción.

### F6 Bloque B · Lote 1 pulido post-testeo (3 bugs + contraseñas + diseño) — hecho
- **Bug "＋ Imputar hoy" invisible en escritorio**: causa raíz confirmada antes de tocar código — `InicioEscritorio.tsx` nunca tuvo cabecera (ni saludo ni CTA) y `ShellEscritorio.tsx` no tenía equivalente de escritorio del FAB móvil; el composer de Imputar ya estaba bien (solo se ocultaba por las dos reglas de negocio legítimas: ausencia aprobada / sin proyectos asignados). Fix: cabecera nueva en `InicioEscritorio.tsx` (saludo + "＋ Imputar hoy" → `ctx.setTab('imputar')`), sin duplicar mapa/avatar del mock (ya viven en el sidebar persistente — divergencia deliberada).
- **Bug "Panel admin" visible para todos**: `ShellEscritorio.tsx` renderizaba el enlace sin comprobar `ctx.rol`. Guardia añadida (`admin_empresa`/`admin_grupo`); guardia de servidor en `/admin/layout.tsx` verificada, ya existía y sigue intacta.
- **Bug categorías sin filtrar por departamento**: migración `015_categoria_departamento.sql` añade `categoria.departamento_id` (nullable, FK a `departamento`) + backfill del seed (Desarrollo/Diseño → 3B3, Abogados → Jurídico, Gestión global) — aplicada a la demo con `supabase db push`. `useCategoriasTareas(departamentoId)` filtra globales + propio departamento (sin departamento, ve todo, sin cambios); único punto de filtrado para composer escritorio, precargado y hoja móvil (los tres consumen `ctx.grupos`). Imputación directa de admin (`ControlEscritorio.tsx`) filtra por separado usando `useUsuarios().departamentoId` del empleado seleccionado. Sección Categorías del admin gana selector de departamento opcional en el alta + etiqueta en el catálogo.
- **Contraseñas**: "Cambiar contraseña" en el menú de usuario (los 4 shells, vía `MenuUsuario.tsx` compartido) — verifica la actual con `signInWithPassword` antes de `updateUser`. "Restablecer contraseña" en la ficha de usuario (`restablecerPasswordEmpleado`, `src/app/admin/actions.ts`) — genera temporal legible (`Palabra-1234`), la muestra una vez, mismo guardarraíl admin_grupo/admin_empresa que `invitarUsuario` (resuelto en TS porque `service_role` bypassa RLS). "¿Olvidaste tu contraseña?" en `/login` + `/reset-password` nuevo, reutilizando `/auth/callback` ya probado con magic link.
- **Control → ficha**: fila de la tabla de faltantes (`.row-link`, mismo patrón que Usuarios/Proyectos/Empresas) navega a la ficha del empleado vía el hand-off genérico (`irA('usuarios', {usuarioId})`); el botón "Imputar" de la fila sigue precargando el formulario de imputación directa sin disparar la navegación (guardia `closest('button')`).
- **Diseño**: inputs pasan a fondo blanco con borde fino (`--bg-surface` + `1px solid var(--border)`, deshabilitados en `--bg-subtle`/`ink-tertiary`) — la primera versión (solo `background`, sin borde) resultaba invisible contra una card del mismo fondo; corregido tras aviso, mocks también corregidos a "blancos con borde fino". "Acciones sobre el usuario" sube justo debajo de los KPIs en la ficha. Tema pasa a ítem del menú de usuario ("Cambiar tema") en los 4 shells; se retira el botón suelto (`ThemeToggle` standalone eliminado de las 4 cabeceras/sidebars, mocks conservan el suyo como affordance de demo, divergencia documentada).
- **Verificado**: build limpio (`npm run build`); lint sin regresión (82 problemas ya existían en `main` antes de este lote, mismos 82 después); migración 015 aplicada a la demo (`supabase db push`); ningún empleado sembrado está en el departamento "Diseño" (comprobado por consulta directa — no hacía falta mover a nadie). Ciclo completo contra la demo local apuntando al Supabase real: Leo (empleado, 3B3) ve el botón Imputar hoy, no ve Panel admin, selector de tarea limitado a Desarrollo/Diseño/Gestión; Andrés (3B3) igual; Ana Ruiz (Jurídico) limitada a Abogados/Gestión, también en imputación directa de admin; Cristian y Marina sí ven Panel admin. Cambio de contraseña propio: contraseña actual errónea rechazada sin mutar nada, correcta cambia y permite entrar con la nueva, revertido a `Horas2026!`. Restablecer por admin: Cristian genera temporal para Andrés, login con la temporal funciona, revertido; Marina (admin_empresa) no ve el botón para Andrés (fuera de su empresa) pero sí para Enrique (su empresa) — guardia de alcance confirmada en la UI. `/reset-password` con sesión activa muestra el formulario correcto (rama sin sesión es un ternario trivial, no probada en vivo). Categorías admin: alta con departamento y catálogo con etiqueta verificados contra el mapeo real. Control→ficha: clic en fila navega, clic en botón sigue abriendo imputación directa sin doble disparo. Capturas de inputs contra los mocks (claro y oscuro) — contraste confirmado en oscuro. `olcasan08@gmail.com` (cuenta real del usuario) no se tocó en ningún momento de la verificación.

### F6 Bloque B · Lote 1.5 — Estructura de URLs — hecho
- **Antes**: toda la navegación vivía en `useState` (`seccion`, fichas seleccionadas, `tab`): atrás rompía, sin enlaces profundos, F5 devolvía al inicio. **Ahora la URL es la única fuente de verdad**; sección, ficha y pestaña se derivan de `usePathname()`.
- **Esquema**: empleado `/inicio|/imputar|/calendario` (`/` → `/inicio`); admin `/admin/<seccion>` (ids de `secciones.ts`; `/admin` → `/admin/inicio`); fichas `/admin/{usuarios,proyectos,empresas}/<id>`; hand-offs efímeros `/admin/control?empleado=<id>` y `/admin/usuarios?invitar=<empresaId>` (se aplican y se limpian con `replaceState`). Modales, hojas, menú de usuario, mapa y confirmaciones NO van a la URL.
- **Implementación sin reestructurar shells**: catch-all opcional por lado (`src/app/[[...seccion]]/page.tsx`, `src/app/admin/[[...seccion]]/page.tsx`) montando el mismo shell. Definición única de rutas en `src/lib/nav/rutas.ts` (`Record<SeccionAdmin, …>`: añadir sección sin decidir su ruta no compila), `navegar.ts` (push/replace) y `ruta-segura.ts`. `NavAdmin.tsx` (contexto) sustituye el hand-off genérico de estado del shell. Rutas específicas (`/login`, `/api`, `/debug*`, `/reset-password`, `/auth`) ganan al catch-all (verificado).
- **Desviación consciente de "navegar = router.push"**: se usa `history.pushState/replaceState` (integrados con el router de Next 16: sincronizan `usePathname`/`useSearchParams` y `popstate` lo gestiona el router). `router.push` a una página dinámica = viaje RSC + `getUser()` del middleware en cada clic de pestaña/sección. El servidor sigue validando en carga fría/F5/enlace profundo.
- **Guardas**: ficha con datos cargando → espera (nunca redirige); cargado y sin el id (o RLS no lo muestra) → `replace` al listado + toast. Segmento desconocido → base del lado (server `redirect`). Sin sesión → `/login?next=<ruta+query>`; `next` validado como ruta interna (`rutaInternaSegura`) en login y en `/auth/callback` (que además tenía un `next` sin validar: open-redirect, cerrado). `admin/layout.tsx` ya no redirige sin sesión (no conoce la ruta y perdería el enlace profundo): lo hace la page.
- **Hallazgo real de la verificación (atrás tras logout)**: el navegador restauraba desde la bfcache la pantalla admin del usuario anterior, con su menú abierto y sus datos. Corregido con `GuardaBfcache` (root layout): un `pageshow` con `persisted` oculta y recarga → decide el servidor. En dev, además, `Cache-Control: no-cache` (no `no-store`) dejaba a "atrás" reutilizar el HTML de otro usuario; en producción las páginas dinámicas ya salen con `private, no-cache, no-store`.
- `/debug/movil*` (solo dev) usan navegación aislada en estado local: no viven bajo esas rutas.
- **Verificado (local, navegador real)**: a) listado→ficha→atrás en usuarios/proyectos/empresas; b) empresa→proyecto→atrás→ficha empresa→atrás→listado; c) carga directa en ficha y en `/imputar` se queda; ficha inexistente → listado + toast; e) `?empleado=` y `?invitar=` aplican, limpian, y atrás/adelante no los re-aplican; f) logout multi-identidad (Cristian→Andrés): atrás nunca muestra datos del anterior; g) móvil emulado con iframe de 390 px (pestañas empleado, drawer admin, aviso "vive en escritorio" en URL de ficha); rutas desconocidas → base. **No verificado por mí**: login real con `?next=` (exige teclear contraseña) y el gesto de atrás de un Android real.

### F6 Bloque B · Lote 3 — Importadores y exportadores (usuarios, coste/hora, vacaciones) — hecho
- **Patrón común, una sola implementación** (`src/lib/importadores/`): plantilla xlsx descargable (hoja 1 con SOLO cabeceras exactas, sin fila de ejemplo; hoja 2 "Instrucciones" con columna, obligatoriedad, valores admitidos, ejemplo y reglas), lectura con exceljs **en servidor**, flujo en dos pasos siempre (subir → **análisis en seco** con informe fila a fila → "Importar N filas" solo con CERO errores; todo-o-nada), UI única `ImportadorBloque.tsx`, y una sola pareja de server actions (`analizarImportacion`/`ejecutarImportacion`, `src/app/admin/importadores.ts`). Cada importador aporta solo plantilla + reglas + mutación vía `definir()`. El servidor **re-parsea y re-analiza el archivo en cada llamada** (no se fía de un análisis previo del cliente). Ámbito **solo admin_grupo**: guard en servidor (`contextoAdminGrupo`, también en las rutas `/api/plantillas/[tipo]`, `/api/export/usuarios`, `/api/export/coste`: 403 para admin_empresa, empleado y anónimo) además de ocultar las tarjetas. Identificador de correspondencia: **email**, nunca nombre. Cabeceras: tolera mayúsculas/acentos pero rechaza columnas desconocidas (un «departamneto» no se ignora en silencio). Límites: 2 MB / 2.000 filas.
- **Usuarios** (tarjeta "Importar / Exportar" en la sección Usuarios): export xlsx (nombre, email, empresa, departamento, categoria, rol, activo) reimportable tal cual (`activo` se ignora → todo duplicados, cero altas). Solo ALTA; email existente = error de fila. **Misma mutación que `invitarUsuario`**: se extrajo `altaUsuario()` (`src/lib/usuarios/alta.ts`) y ambos la usan. Todo-o-nada por **compensación** (un usuario de auth no cabe en una transacción de Postgres): ante el primer fallo se borran las cuentas creadas en el lote (el perfil cae en cascada) — verificado con un email "huérfano" (existe en auth, no en perfil) que pasa el análisis y falla a mitad de lote.
  - **Alta de usuario = un solo comportamiento, manual o en lote (decisión cerrada)**: `invitarUsuario` (alta de una persona desde el formulario) y el importador comparten `altaUsuario()` **y** el mismo modo, `modoAltaDesdeEntorno()`:

    | Entorno | `MODO_EMAIL` | Qué ocurre al dar de alta | Cómo accede la persona |
    |---|---|---|---|
    | **Demo** | no definida o ≠ `real` (Railway hoy: sin definir) | `createUser` con `email_confirm:true`: cuenta confirmada, **sin contraseña y sin correo** (como las 11 cuentas de demo) | Un admin usa **"Restablecer contraseña"** en su ficha (temporal mostrada una vez) |
    | **Producción real** | `real` | `inviteUserByEmail`: invitación real por email | La persona la recibe y fija su propia contraseña |

    Motivo: en demo la invitación manual anterior rebotaba en silencio contra buzones inexistentes (@wowinx.com) y consumía el límite del SMTP de Supabase; dos comportamientos según se haga de una en una o en lote era una divergencia. La UI dice la verdad: `invitarUsuario` devuelve el `modo` y el aviso es "Invitación enviada a …" o "Cuenta creada … (sin correo): dale acceso con «Restablecer contraseña»". El envío real con `MODO_EMAIL=real` no se ha probado (sin SMTP propio): queda en el runbook de producción.
    **Verificado en producción (redeploy `4ec3140`)**: alta manual por el formulario → perfil + auth creados, cuenta confirmada y `invited_at` nulo (sin invitación) → "Restablecer contraseña" desde su ficha → la temporal da sesión (y una contraseña incorrecta no) → cuenta revertida; conteos de las 17 tablas y `auth.users` idénticos antes/después. No capturé el texto exacto del aviso en pantalla (el toast desaparece antes de leerlo).
  - ~~Producción real: `MODO_EMAIL=real` + SMTP propio~~ **Superado por la decisión "sin email" del combinado 5+2 (abajo)**: producción tampoco envía correo; el modo `invitar` queda dormido en el código.
- **Coste/hora** (migración **016**, concepto nuevo: coste INTERNO del empleado para control salarial — NO la tarifa de refacturación): `coste_empleado(id, perfil_id, coste_hora ≥ 0, desde, creado_en)`, único `(perfil_id, desde)`, versionada: importar añade fila, nunca machaca; repetir versión = error de fila. Vigente = mayor `desde` ≤ hoy (`v_coste_vigente`, `security_invoker` — sin eso la vista saltaría la RLS; "hoy" en hora de Madrid). **RLS estricta: select y escritura solo admin_grupo** (`for all` con `es_admin_grupo()`); ni admin_empresa lo ve. Import: email, coste_hora (coma o punto), desde (opcional, defecto hoy); export del vigente por usuario. Visible en UN solo sitio: ficha de usuario, "Coste/hora vigente … desde …", renderizado y consultado SOLO para admin_grupo. Los informes salariales que lo crucen con horas son una pieza futura.
- **Vacaciones → Ausencias** (migración **017**, `importar_ausencias(jsonb)`; tarjeta en la sección Ausencias): nacen `aprobada` con el admin importador como `resuelta_por` (hechos consumados del plan anual, documentado en las Instrucciones), comentario "Importada (plan anual)". Una sola función = **una transacción**: todo-o-nada garantizado por Postgres, no por compensación (verificado: solape interno rechazado por el trigger → no queda ni la primera fila). Mismo efecto que `aprobar_ausencia` (borra los borradores de esos días). Análisis: fechas reales, inicio ≤ fin, rango ≤ 366 días, **solape con ausencias pendientes/aprobadas existentes del empleado o entre filas del propio archivo → error de fila** (resolver solapes es manual), y — decisión cerrada, coherente con "nunca pisa" — **error si ya hay horas computadas/aprobadas/cerradas en el rango** (vacaciones sobre esos días corromperían las requeridas efectivas, la familia de la 014); documentado en las Instrucciones de la plantilla ("días con horas ya registradas dan error — resuélvelo antes en la app"). El RPC lo llaman solo admin_grupo (Marina y Andrés: "Solo admin_grupo puede importar ausencias").
- `src/lib/supabase/types.ts` regenerado (`supabase gen types`): solo añadidos (`coste_empleado`, `v_coste_vigente`, `importar_ausencias`).
- **Verificado (local, contra el Supabase de demo; sesiones reales)**: plantillas y exports descargados y abiertos con exceljs (cabeceras + Instrucciones); guards 403; usuarios: fila errónea cazada en análisis ("fila 3: empresa 'Wowinks SL' no existe", sin botón Importar y conteos intactos), archivo corregido importa 3 altas con empresa/departamento/categoría/rol correctos, **round-trip** exportar→reimportar = 16/16 duplicados y 0 altas, compensación probada, **reversión completa** (auth + perfil); coste: dos versiones del mismo empleado + una futura, vigente correcto en la ficha (35,50 €/h desde 01/06/2026, no el 30 ni el de 2027), **visible como Cristian, invisible como Marina** (DOM sin el dato y RLS: 0 filas en tabla y vista incluso para un empleado de su propia empresa), reversión; vacaciones: caso transversal 28/09–02/10 de Sara (3 días en sept, 2 en oct, recorte por mes del helper de la 014), solape rechazado en análisis, y **regresión byte-idéntica**: foto SHA-256 de `fte_mes`/`balance_mes_empleado`/`estado_dias_mes` sep+oct antes = tras importar y revertir = tras aprobar a mano y revertir, y la foto "importada" = la foto "aprobada a mano" (0/30 claves distintas).

- **Retest de un ciclo completo contra PRODUCCIÓN (redeploy Railway `SUCCESS`)**: plantillas/exports descargados y abiertos (cabeceras + Instrucciones) y 403 para Marina/Andrés; usuarios (error cazado → corregido → 3 altas → round-trip 16/16 duplicados → reversión auth+perfil); coste (2 versiones + futura → ficha 35,50 €/h desde 01/06/2026 como Cristian, invisible como Marina, RLS con sesión real → 0 filas); vacaciones (4 errores cazados, importación transversal 28/09–02/10, foto de RPCs idéntica a la de local y a la de "aprobada a mano", revertida); hand-offs `?empleado=`/`?invitar=` reverificados tras el refactor. **Seed exactamente como estaba**: conteos de las 17 tablas + `auth.users` idénticos antes/después (perfil 13, auth 13, ausencia 8, imputacion 139, coste_empleado 0…) y foto SHA-256 de los RPC de horas `756660c9…` antes = después (nota: local y producción comparten el mismo proyecto Supabase de demo, así que la base de datos es una sola).
- **Limitaciones de la verificación (no ocultar)**: sesiones de navegador por inyección de cookie desde magic link (no se teclean contraseñas); la subida de archivos se hizo con la herramienta de subida del navegador; no se probó el envío real de invitaciones (`MODO_EMAIL=real`, sin SMTP propio); `Restablecer contraseña` sobre una cuenta importada no se re-ejecutó (mecanismo ya verificado en el Lote 1).

### F6 Bloque B · Combinado 5+2 — Soporte + modo sin email + jornada semanal y resúmenes de día — hecho (migraciones 018 y 019)

**A. Decisión de producto — SIN EMAIL (definitiva; cierra la cuestión SMTP)**
- **Dos caminos de alta, ambos sin correo** (`src/lib/usuarios/alta.ts`, una sola mutación `altaUsuario`):

  | Camino | Qué hace | Cómo accede la persona |
  |---|---|---|
  | **Alta manual** (Usuarios → formulario, botón "Crear usuario") | Campo **"Contraseña inicial"** (opcional, mín. 8) con botón **Generar** (patrón `Palabra-1234`, mismo generador que "Restablecer contraseña": `src/lib/usuarios/password.ts`, con `crypto`). Tras crear, un aviso muestra la contraseña UNA vez para copiarla y entregarla en mano. Cuenta confirmada (`createUser` con `password`). Si se deja vacío, nace sin contraseña | Contraseña inicial entregada en mano; después la cambia desde el menú del avatar |
  | **Importador** (Usuarios → Importar / Exportar) | Sin cambios: cuentas confirmadas SIN contraseña | Un admin usa **"Restablecer contraseña"** en la ficha (temporal mostrada una vez) |

- Divergencia deliberada del mock: el botón "Enviar invitación" pasa a **"Crear usuario"** (no se invita a nadie); el título de la tarjeta se deja como el mock.
- **Bug real hallado en la verificación**: el "Generar" metía el azar dentro del updater de `setForm`; React (StrictMode) lo invoca más de una vez, así que el campo mostraba una contraseña y la cuenta se creaba con OTRA (`Alba-3076` en pantalla / `Roble-6638` en la cuenta). Corregido (el azar se genera fuera del updater) y repetido: campo = aviso = contraseña real, login OK.
- **Correo dormido, no retirado**: invitaciones, recovery de `/login` (inerte también en producción: se cambia la contraseña desde el avatar) y recordatorios (cron y botón Recordar siguen en modo `log`: registran sin enviar). Runbook: SMTP/Resend/`MODO_EMAIL=real` dejan de ser prerrequisitos y pasan a "Futuro opcional: activar email".

**B. Soporte (migración 018)**
- Esquema: `ticket` (`ref` T-001… **generada en BD** con secuencia; `creado_por`, título, descripción, `tipo` incidencia|mejora|consulta, `estado` abierto|en_curso|resuelto) y `ticket_comentario`. **RLS**: empleado (y responsable) crea tickets a su nombre y comenta en los SUYOS y solo ve los suyos; admin_empresa ve y comenta los de su empresa (empresa del creador); admin_grupo todo; **cambiar estado solo admins**; **sin delete para nadie**. Reforzado con **privilegios por columna** (el cliente no puede fijar `ref`/`estado`/fecha al crear ni tocar más que `estado` al actualizar). Un ticket `abierto` pasa a `en_curso` cuando responde un admin que no es su autor (trigger: una sola vía, atómica). `ticket_hilo()` (security definer) sirve el hilo con el NOMBRE del autor: un empleado solo ve su propio perfil, así que un join directo no le mostraría quién le responde.
- **Hallazgo de la verificación → corregido en la 019 (sección 0)**: la policy `ticket_select` de la 018 releía `ticket` desde `puede_ver_ticket(id)`; el `RETURNING` de un INSERT no veía su propia fila y el alta legítima de un empleado fallaba con "violates row-level security". Ahora evalúa la fila directamente. También `ticket_ref_resincronizar()` (solo service_role): la secuencia no es transaccional y tras limpiar pruebas la siguiente ref saltaría.
- Lado empleado (los 4 shells, `MenuUsuario.tsx` compartido): "Soporte" entre "Cambiar contraseña" y "Cerrar sesión" → modal (escritorio) / hoja (móvil) con mis tickets, hilo + comentar y "Nuevo ticket" (tipo, título, descripción). **Sin mock: esta spec es la fuente de verdad.** El empleado no cambia estados.
- Lado admin: sección **Soporte** en Operación tras Control (`/admin/soporte`, `/admin/soporte/<id>`), réplica del mock: filtros, tabla `row-link`, ficha con hilo, Responder, Marcar en curso/resuelto/Reabrir. Admin móvil: pantalla **funcional** en el drawer (lista de tarjetas + ficha con respuesta y estado). Badge con el nº de tickets abiertos junto a "Soporte" (recuento simple al cargar; lo acota la RLS). Seed demo: T-014..T-017 literales del mock con sus hilos (guardado: solo si existen los perfiles demo; la siguiente ref real es T-018). Divergencia menor: el mock rotula "Jueves 11 sep" (calendario de otro año); aquí el día de la semana sale de la fecha real (viernes).

**C. Jornada semanal + resúmenes de día (migración 019 — núcleo delicado)**
- `empresa_jornada` (7 filas por empresa, `unique(empresa_id, dia_semana)`; lectura abierta, escritura solo admin_grupo; toda empresa nueva nace con sus 7 filas por trigger).
- **DESCUBRIMIENTO que corrige la spec**: `ajuste.jornada_horas` vale **8** en la demo viva (alguien lo cambió desde el panel), no 7. Sembrar un 7 fijo habría movido todas las cifras. **Seed = comportamiento actual**: L-V toman el valor VIVO de `jornada_horas()` (8) y S-D 0. Las cifras "154 h / 172·140·+32 / 1,2286 / 2,2772" son las de jornada **7** (demo antigua): con 8 la requerida de sept es 176 y Andrés 172/160/+12 → FTE 1,0750, Ximeras 1,9925. **Demostrado**: poniendo temporalmente L-V=7 el nuevo modelo reproduce EXACTAMENTE 154 / 172 / 140 / +32 / 1,2286 / 2,2772; se revirtió a 8 y la foto quedó idéntica.
- **Helper único `jornada_del_dia(empresa, fecha)`** (sin fila propia → defecto global); `es_laborable` = jornada > 0 y no festivo; `horas_requeridas` = SUMA de la jornada de cada día laborable.
- **Inventario citado en la propia migración**. *Parcheadas* (usaban jornada plana por día): `faltantes()` (002), `faltantes_recordatorio()` (012), `resumen_dia()` y `resumen_mes()` (010), `estado_dias_mes()` (011). *Generalizadas en su definición*: `es_laborable`, `horas_requeridas` (→ `horas_requeridas_mes` → `balance_mes`, `balance_mes_empleado`, `fte_mes`). *No tocadas, con cita*: `balance_mes`/`balance_mes_empleado`/`fte_mes` (014: solo usan `horas_requeridas_mes` y `dias_ausencia_en_mes`, que cuenta DÍAS laborables y es correcta con cualquier jornada; su forma de salida no cambia), `v_ausencia_dias`, `cerrar_periodo` (011: no usa jornada ni laborables). *Nuevo*: `horas_ausencia_en_mes()` y `requeridas_efectivas(anio, mes)`: la deducción de ausencias pasa de "días × jornada plana" (TypeScript) a la SUMA de la jornada de cada día cubierto — un viernes de 5,5 h descuenta 5,5. Una sola función sirve al propio empleado, la ficha y el export FTE (`src/lib/horas/requeridas-efectivas.ts` queda como la resta única).
- **Réplica en TypeScript eliminada**: `construirDiasMes` (fin de semana cableado) → `jornada_dias_mes()` (RPC); los calendarios del empleado usan `dia.jornada` en vez de una jornada plana.
- **REGRESIÓN byte-idéntica**: foto SHA-256 ampliada (73 claves: `fte_mes`, `balance_mes_empleado` de los 13 perfiles, `estado_dias_mes`, `resumen_mes`, `resumen_dia` en 9 fechas, `faltantes`, `faltantes_recordatorio`, sept+oct, con sesiones reales de Cristian y de Marina) **antes = después de aplicar la 019 = después del caso 4 = después del cambio por la UI = después de la prueba con 7 plano**: `a97033ad…`. La fórmula vieja (días × jornada) y `requeridas_efectivas()` coinciden en 21/21 empleado-mes (jul-oct).
- **CASO NUEVO (revertido)**: Wowinx a 8/8/8/8/5,5 → sept 2026 = **166,0 h** (18 días L-J × 8 = 144 + 4 viernes × 5,5 = 22). **La spec decía 176,0: es un error de suma (144+22 = 166); 176 es justo el plano de 8 h, o sea "no ha cambiado nada".** Octubre 155,5 (16×8 + 5×5,5, festivo 12/10). Andrés: requeridas 166 − baja mar 1 + mié 2 (2×8) = **150** efectivas; y con una vacación aprobada en VIERNES 25 la deducción es 5,5 (efectivas 160,5), donde la fórmula vieja habría dado 158. `faltantes` del 11-sep pasa a requerir 5,5 h a Wowinx y 8 h al resto; `resumen_dia` igual. Málaga intacta (176). Verificado por SQL y por la UI (tabla "Horas requeridas por mes": 166,0 / 155,5, refresco tras guardar).
- **UI** (Calendario admin): card "Jornada semanal" del mock (select de empresa, 7 campos con coma decimal, total semanal en vivo `37,5 h/sem`, "Guardado ✓"); solo admin_grupo escribe (admin_empresa la ve deshabilitada; RLS comprobada con intentos reales de Marina y Leo). La tabla "Horas requeridas por mes" sigue a la empresa elegida y cuenta laborables desde la BD.
- **Resumen del día**: clic en un día de la Vista del mes (ahora con estados reales de `estado_dias_mes`, findes pulsables) → card como el mock; **el día va a la URL `/admin/calendario?dia=YYYY-MM-DD`** (F5 lo conserva, atrás/adelante funcionan). El ESTADO Al día/Falta/Ausencia lo decide `resumen_dia()` (mismas personas que "Faltantes" de Control: el 11-sep, 8 personas coinciden exactamente); horas y proyectos se agregan **en cliente** desde `perfil`/`imputacion` con la RLS del admin (sin SQL nueva). Límite conocido: un admin_empresa solo ve las imputaciones de proyectos de su empresa, así que el desglose de alguien suyo que imputó en otra puede quedarse corto (el estado no). Marina solo ve a su gente (2 de 2).
- **Mini-calendario en la ficha de usuario** (tras Acciones): rejilla mensual con estado por día (al día / incompleto / ausencia / no laborable / futuro), detalle efímero al clic con las líneas y su estado, mes navegable. **Cero SQL nueva**: días vía `jornada_dias_mes`, líneas de `imputacion`, ausencia de las ya cargadas por la ficha. Solo con datos en ámbito (un intragrupo no lo ve).
- **Hallazgo en la verificación en producción**: el panel "Soporte" del avatar de un ADMIN listaba TODOS los tickets del sistema (la RLS de admin_grupo/admin_empresa los deja pasar) bajo el título "Mis tickets". Es exactamente el patrón de la norma de "lo mío" (§3.1): corregido con `useTickets({ soloMios: true })` (`.eq('creado_por', uid)` explícito); la sección Soporte del admin sigue viendo todo su ámbito.
- **Verificado (local)**: ciclo Soporte con sesiones reales — Leo crea T-018 desde su móvil (iframe 390 px; ve solo los suyos) → badge de Cristian 2→3 → Cristian responde (pasa a en curso, badge 3→2) → Leo ve la respuesta y el estado, no tiene botones de estado, comenta → Cristian resuelve → Leo lo ve resuelto (sesión real); Marina solo ve T-016 (badge 1) y no puede abrir el de Ana; intentos REALES rechazados por RLS (Leo: cambiar estado 0 filas, título/borrar/ref/estado forzados → permission denied, comentar ajeno o como otro → violates RLS; anon → denied); refs seguidas sin colisión (T-021/T-022 en la prueba); URLs `/admin/soporte/<id>` con atrás/adelante/F5; datos de prueba borrados y secuencia resincronizada (siguiente = T-018); los 4 del seed intactos. Alta con contraseña: creada, login real con la generada, revertida. Mini-calendario de Andrés: baja 1–2 sep, detalle del 10 (8,0 h Ximeras, Aprobada), fin de semana, futuro, mes navegable; KPIs de la ficha idénticos (179,5 / 160,0 h, +19,5, 2 días).

- **Verificado en PRODUCCIÓN (redeploy Railway `SUCCESS`)**: la foto SHA-256 ampliada (73 claves) sigue en `a97033ad…` tras el deploy; el export FTE desplegado da Andrés 1,0750 y Ximeras 1,9925 (jornada viva de 8 h) y los KPIs de su ficha 179,5 / 160,0 h / +19,5; Calendario: caso 8/8/8/8/5,5 por la UI desplegada → sept **166,0** / oct **155,5**, revertido a 8 plano y foto idéntica de nuevo; resumen del 11-sep con `?dia=` tras F5 (8 personas en "Falta", las mismas que Control); mini-calendario de Andrés (baja 1–2 sep, detalle del 10); alta manual con contraseña generada (campo = aviso = contraseña real, login real OK, revertida); ciclo de Soporte (alta desde la hoja móvil, respuesta de Cristian con sesión real → en curso, ficha admin con hilo, comentario y "Marcar resuelto" desde la UI desplegada; T-018 borrado y secuencia resincronizada). En producción usé la sesión de Haizea (admin_grupo) ya abierta en el navegador, sin sobrescribirla; las identidades Leo/Marina se verificaron en local con sesiones reales y por RLS con intentos reales.
- **Conteos**: antes y después idénticos en las 17 tablas previas y `auth.users` (perfil 13, ausencia 8, imputacion 139, coste_empleado 0…); las tablas nuevas quedan `ticket` = 4 (T-014..T-017), `ticket_comentario` = 4, `empresa_jornada` = 21 (3 empresas × 7). El mock `mocks/panel_administracion.html` (versión acumulada) va en el commit del combinado.

### Normas de verificación fijadas tras el combinado 5+2 (2026-09-19)
- **Cifras canónicas de regresión = las de la jornada viva de 8 h** (`ajuste.jornada_horas` = 8): requeridas de sept **176 h**, Andrés **172 / 160 / +12**, FTE Andrés **1,0750**, Ximeras **1,9925**, foto SHA-256 ampliada (73 claves) **`a97033ad…`**. Las cifras de jornada 7 (154 / 172-140-+32 / 1,2286 / 2,2772) son histórico de la demo antigua y ya no sirven de regresión. Del caso 8/8/8/8/5,5 el valor correcto de sept es **166,0 h** (18×8 + 4×5,5): el "176,0" que decía la spec era una errata aritmética (176 es la jornada plana de 8 h).
- **Toda verificación arranca con login explícito de una identidad de la plantilla** (script: magic link → cookie, o cliente con sesión real vía `verifyOtp`), sin excepciones, también cuando el navegador traiga sesiones del usuario. La norma no es "no sobrescribas la sesión abierta": es **no usarla**. Un verificado con la sesión que hubiera en el navegador no cuenta como verificado. Al terminar se borran las cookies `sb-*`.

### Correctivo del combinado 5+2: lado empleado del ciclo de Soporte EN PRODUCCIÓN con login explícito de Leo (2026-09-19) — hecho
- **Motivo**: la verificación en producción del combinado se había hecho con la sesión de Haizea que traía el navegador, y el lado empleado (Leo) solo se había probado en local. Repetido ahora en `timerx-production.up.railway.app`.
- **Login explícito**: magic link de `leo.silva@wowinx.com` (script `magic-link.mjs`, `redirectTo` = dominio de Railway) → cookie de sesión construida desde el hash. El navegador ya traía una sesión previa (Haizea: aterrizó en `/inicio`, sin pasar por `/login`); **no se usó**: se borraron las cookies `sb-*` y se escribió la de Leo. Identidad comprobada en pantalla antes de operar ("Hola, Leo", menú "Leo Silva · leo.silva@wowinx.com · Empleado", sin enlace "Panel admin"). Trampa anotada: quitar el `#hash` de la URL es navegación en el mismo documento, no recarga — el DOM seguía siendo el de la sesión anterior hasta forzar una carga completa (`/imputar`).
- **Ciclo (UI desplegada como Leo; Cristian por script con sesión real `verifyOtp`)**: Leo ve solo su T-014 → **crea T-018** desde "Nuevo ticket" (toast "Ticket T-018 creado", en su lista como Abierto: el alta legítima de un empleado, que fallaba por RLS antes de la 019, funciona en producción) → Cristian responde → estado **en_curso** (trigger) → Leo ve la respuesta con el nombre del autor ("Cristian Haro · Soporte"), estado En curso y sin botones de estado → Leo **comenta** (2 respuestas) → Cristian marca **resuelto** → Leo lo ve **Resuelto** en su lista tras recargar.
- **Reversión**: T-018 y sus 2 comentarios borrados con service_role; `ticket_ref_resincronizar()` → 17 (siguiente = T-018); conteos de las 21 tablas + `auth.users` **idénticos** antes/después; T-014..T-017 en su estado original (resuelto/en_curso/abierto/abierto). Cookies `sb-*` del navegador borradas y pestaña cerrada. **Efecto colateral asumido**: la sesión de Haizea que había en ese navegador para el dominio de producción ya no existe (habrá que iniciar sesión de nuevo allí).
- **Aclaración "Haiba"**: no existe ninguna cuenta con ese nombre ni parecida en `perfil` ni en `auth.users` (13 y 13 revisadas, ver tabla de cuentas en §6). La única cercana es **Haizea** (`haizea@wowinx.com`, admin_grupo legítima): "Haiba" es una errata.

### F6 Bloque B · Lote 4 — Tipologías, color por área, edición inline y barrido de pulido (migración 020) — hecho
- **Tipologías (020)**: `empresa.area_id` y `proyecto.area_id` (uuid NULL → `mapa_area`). **La tipología ES el área del mapa**: un solo catálogo, cero enums. Al crear un proyecto, "Heredar de la empresa (X)" (por defecto) **copia** el área de la empresa EN el proyecto (no es referencia viva: cambiar después la tipología de la empresa no recoloca proyectos ya creados; verificado); además, las áreas y "Sin tipología". Empresa: select en el alta y en "Editar datos" de la ficha (solo admin_grupo, como el resto de la escritura de empresa); la ficha muestra su tipología en la cabecera. **Añadido no previsto por el mock**: select de tipología en la ficha de proyecto (sin él no había forma de cambiarla tras crearlo); deshabilitado salvo admin_grupo o admin_empresa de la empresa del proyecto (misma regla que `proyecto_admin`). Seed: Wowinx→Tecnología, Málaga→Deportes, Legal Norte→NULL (a propósito: demuestra el fallback), proyectos heredan salvo Interno y Asesoría intragrupo (NULL); en la migración (BD viva) y en `seed.sql` §7 (BD nueva). RLS: sin policies nuevas, las columnas heredan las de su tabla; intentos reales de Marina: `empresa.area_id` 0 filas, `area_id` de un proyecto de Wowinx 0 filas, del de su empresa 1 fila.
- **Mapa semi-automático**: composición en LECTURA dentro de `useMapa` (sin tabla nueva): los proyectos ACTIVOS con `area_id` aparecen bajo su área, DESPUÉS de los manuales, con etiqueta **AUTO** y la descripción fija del mock. Deduplicación: si el área ya tiene un manual con el mismo nombre, el automático no se pinta (comparación sin distinguir mayúsculas ni espacios de los extremos: probado con «prueba lote4 alfa » frente a «Prueba Lote4 Alfa»). Hoy en la demo: Masterchef y Web corporativa salen AUTO en Deportes; Ximeras/Triatix/Launcher NO se repiten (ya son manuales de Tecnología). La gestión del mapa en admin no cambia.
- **Color por área en los roscos**: `src/lib/mapa/colores.ts` (`coloresRosco`), UNA función para empresas y proyectos: base = color del área; los siguientes de la misma área DENTRO del mismo rosco degradan con `color-mix` hacia `--bg-surface` (22 puntos por posición, **suelo del 34 %**: divergencia del mock, que no acota y daría porcentajes negativos con muchos proyectos por área); sin tipología → los grises del mock alternando. Aplicada a TODOS los quesos por empresa/proyecto: Inicio admin (escritorio: empresa y proyecto; móvil: proyecto), Empresas y Proyectos. La ficha de proyecto ya usaba los colores de categoría (`CAT_COLOR`), no grises; el empleado no tiene roscos por proyecto. Las leyendas llevan siempre nombre + porcentaje (el color no es el único portador de significado). `useHorasPorEmpresaYProyecto` devuelve `areaId`; `useAreasTipologia` da las áreas activas (una tipología cuya área se desactiva cae a grises).
- **Edición inline en Usuarios** (`CeldaEditable`): Departamento, Rol y Categoría clicables (title "Clic para cambiar") → select in situ → guarda con `useUsuarios.actualizar` (la misma mutación que la ficha, no una segunda vía) + ✓ efímero. Rol pide `confirmar()` con el efecto ("de «Empleado» a «Responsable de proyecto»: cambia lo que puede ver y aprobar"); departamento y categoría guardan directo. Empresa y nombre NO son editables (puerta única: la ficha). El clic en la celda no abre la ficha (guard `.cell-edit` en la fila). Ámbito = reglas de la ficha: admin_grupo edita todo; **admin_empresa solo edita departamento y categoría de los de su empresa y NUNCA el rol** (en la ficha el select de rol está deshabilitado para no-admin_grupo); fuera de ámbito la celda es normal. **Bug de diseño hallado en la verificación y corregido**: la primera versión mandaba los CUATRO campos de la fila (cacheados) y pisaba cambios hechos por otro admin entretanto (rol y departamento de Leo volvieron a los valores viejos). `ActualizarUsuarioInput` pasa a campos opcionales y `actualizar` escribe solo los presentes: la ficha sigue mandando todos; el inline manda solo el cambiado. Reprobado con un cambio externo simulado: se respeta.
- **Alta rápida de categoría** (formulario de invitación): "＋ Nueva categoría" → input + Crear → misma mutación `crearCategoria` (nace GLOBAL, `departamento_id` NULL: para acotarla, sección Categorías), se autoselecciona y el botón dice "Creada ✓". Solo admin_grupo (la RLS de `categoria` ya lo exige: el intento de Marina da "violates row-level security"; la UI lo oculta).
- **Barrido de backlog**: (a) `empresa.activa` deja de ser decorativa. Selectores que ahora filtran `activa = true` (manteniendo visible la ya elegida): alta de proyecto (Proyectos), empresa empleadora del alta de usuario (Usuarios), empresa de la jornada (Calendario), empresa del select de la ficha de usuario (Editar datos), empresa del elemento del Mapa (gestión), empresa origen de Tarifas y empresa del export de Refacturaciones. Listado de Empresas: filas inactivas atenuadas + badge "Inactiva". Verificado con una empresa de prueba: activa aparece en todos, desactivada en ninguno (control positivo reactivándola), luego borrada (sus 7 filas de jornada caen en cascada). **No tocado**: el importador de usuarios sigue resolviendo la empresa por nombre sin mirar `activa` (no es un selector). (b) Hero del empleado con 0 h imputadas: balance en `ink-tertiary` + "Aún sin imputaciones este mes" (solo presentación, cero cálculo; escritorio y móvil, comprobado con Cosme, 0 h en septiembre).
- **Decisión de alcance cerrada (no construir)**: la bandeja de aprobación de imputaciones y las fichas (usuario, proyecto, empresa) **NO entran en admin móvil en v1**: el admin aprueba y opera fichas en escritorio; el móvil vigila (inicio, ausencias, mapa, soporte). Se retiran las "candidatas" de F5 y de Pulido.
- **Resumen del día para admin_empresa**: micro-aviso en la card ("Las horas de tu gente en proyectos de otras empresas no se incluyen."). **Semántica documentada**: el ESTADO (Al día/Falta/Ausencia) lo decide `resumen_dia()` con todas las personas de su empresa, pero las horas y proyectos se agregan en cliente desde `imputacion` con la RLS de admin_empresa, que solo deja ver imputaciones de proyectos de SU empresa; si alguien suyo imputó en un proyecto de otra empresa, esas horas no cuentan en el desglose. Es consecuencia correcta de la RLS; el aviso evita que parezca un descuadre. (Marina, 11-sep: 2 personas, aviso visible.)
- **Seed T-016**: el texto decía "viernes 26" (calendario del mock); en 2026 el 26-sep es sábado y el viernes es el 25. La app manda en fechas: corregido en la 020 con un `UPDATE` que solo actúa si la fila conserva el texto original.
- **Bug de fechas `toISOString()` — corregido y barrido (`src/lib/fechas.ts`)**: `toISOString()` convierte a UTC; sobre un `Date` construido en hora local (Madrid) devuelve el día anterior. **No era solo latente**: reproducido con `TZ=Europe/Madrid` (el navegador del usuario está en `Europe/Madrid`) y verificado en pantalla tras el arreglo. Helper único, aritmético (`sumarDias`, `ultimoDiaMes`, `primerDiaMes`) y "hoy" del negocio en hora de Madrid (`hoyMadrid`, `fechaMadrid`), igual en navegador y servidor (Railway corre en UTC); probado en 12 casos bajo TZ Madrid, UTC y UTC−4 (incluye cambio de hora y año bisiesto). Inventario de CADA uso, con veredicto:
  - **Corregidos — último día del mes** (`new Date(anio, mes, 0).toISOString()` daba `2026-09-29`, y `lte('fecha', hasta)` dejaba fuera el último día): `useFichaUsuario.ts:48`, `useCalendarioAdmin.ts:53`, `useFichaProyecto.ts:59`, `useHorasPorEmpresaYProyecto.ts:39`, `useAusenciasMes.ts:34`, `useImputacionesMes.ts:75` → `ultimoDiaMes()`.
  - **Corregidos — bug vivo**: `sumarDias()` de `InicioEscritorio.tsx:20-25` y `InicioMovil.tsx:14-19` (Date local + `toISOString`): en Madrid "+1 día" devolvía el MISMO día y "−1" retrocedía dos. Ahora la flecha va 19 → 20 → 19 → 18 (comprobado en el navegador).
  - **Corregidos — "hoy" como fecha UTC** (entre las 00:00 y las 02:00 de Madrid daba AYER): `InicioEscritorio.tsx:28`, `InicioMovil.tsx:22`, `EmpleadoApp.tsx:42` (`fechaHoy`, base de la pantalla del empleado), `ControlEscritorio.tsx:22`, `FichaProyectoEscritorio.tsx:32`, `FichaUsuarioEscritorio.tsx:76`, `TarifasEscritorio.tsx:27` y `:44`, `useAsignaciones.ts:18` y `:27` (fecha de inicio/fin de asignación), `debug/ImputacionTester.tsx:17` (solo dev).
  - **Corregidos — servidor** (ventana de recordatorios, día en UTC): `app/admin/actions.ts:56-60` y `:90-94`, `api/cron/recordatorios/route.ts:13-17` → `hoyMadrid()` + `sumarDias()`; `lib/recordatorios/enviar.ts:19` (`toDateString()` en la zona del proceso: el tope "1 recordatorio por día natural" cortaba en medianoche UTC) → `fechaMadrid()`.
  - **Correctos, no se tocan**: `lib/importadores/fechas.ts:16` (construye con `Date.UTC` y lee con `getUTC*`), `lib/importadores/xlsx.ts:85` (exceljs entrega las celdas de fecha como `Date` UTC a medianoche), `lib/recordatorios/enviar.ts:87` (`ahora.toISOString()` es un `timestamptz` completo, no una fecha), `hooks/useSoporte.ts:37-38` (zona `Europe/Madrid` explícita), `MiniCalendarioUsuario.tsx:33,43` (getters locales sin pasar por UTC), `lib/horas/calendario.ts:39-40,59-60,71-72` y `hooks/useDiasMes.ts:27` (`new Date(f + 'T00:00:00')`/`new Date(y, m-1, d)` con getters locales: consistentes), y los `new Date()` que solo sirven para año/mes (`CalendarioEscritorio`, `EmpresasEscritorio`, `ProyectosEscritorio`, `RefacturacionEscritorio`, `ControlEscritorio`, `FichaUsuarioEscritorio`, `EmpleadoApp`, `InicioEscritorio/Movil`, `debug/page.tsx`). **Límite conocido**: el año/mes del "hoy" siguen saliendo del reloj del navegador (supuesto: usuarios en Madrid); solo las cadenas de fecha son independientes de la zona.
- **Lint**: los 5 avisos nuevos del combinado quedan a cero (`useJornadaSemanal`, `useTicketsAbiertos` y `useSoporte` ×2 → carga fuera del efecto con etiqueta de recurso y `loading` derivado, sin `setState` síncrono en el efecto; `calendario.ts`: `toISO` y `pad` sin uso). Total 84 → **79**; el diff por fichero/regla solo muestra desapariciones, ningún aviso nuevo del Lote 4. Los 79 son preexistentes y no son de este lote.
- **Regresión (cifras canónicas de jornada 8)**: foto SHA-256 ampliada (73 claves) **`a97033ad…` idéntica antes y después de la 020** (con sesiones reales de Cristian y Marina). Tras las pruebas de UI la huella cruda pasó a `d8d7dbcc…` con LOS MISMOS DATOS: **`resumen_dia`/`resumen_mes` no desempatan** las filas con el mismo `imputado` (el orden sale del orden físico de `perfil`) y mis `UPDATE` de prueba sobre perfiles movieron sus tuplas. Se demuestra con la huella **canónica** (arrays ordenados, insensible al orden), `6c50f784`, idéntica en base, tras la 020 y al final. **Norma desde ahora**: la regresión se compara con la huella canónica `6c50f784` (la cruda vigente es `d8d7dbcc`); pendiente, como tarea separada, añadir un desempate a esos `order by`. Cifras: requeridas de sept 176, Andrés 172 imputadas / 176 requeridas (efectivas 160, +12), 40+64+68 h por proyecto. Conteos de las 21 tablas y `auth.users` idénticos antes y después.
- **Verificado en local y en PRODUCCIÓN (redeploy Railway `SUCCESS`, commit `f20dd19`), SIEMPRE con login explícito por identidad** (magic link → cookie tras borrar las `sb-*` previas; identidad comprobada leyendo el email de la sesión antes de operar; cookies borradas al terminar): *Cristian* — navegador de días 19→20→19→18, roscos con color de área (claro y oscuro en local), edición inline real de la categoría de Leo (solo cambia ese campo; revertida), aviso del resumen del día AUSENTE para admin_grupo; *Andrés* — mapa con AUTO Masterchef y AUTO Web corporativa bajo Deportes y sin duplicar Ximeras/Triatix/Launcher (y en local, sin redeploy: proyecto nuevo heredando → AUTO; manual homónimo lo desplaza; cambiar la tipología lo recoloca al instante y recolorea su porción del rosco); *Marina* — inline solo en su gente y solo dep/categoría (nunca rol, sin "Nueva categoría"), aviso visible, ficha de proyecto con tipología deshabilitada en Ximeras y habilitada en Web corporativa, intentos reales por RLS; *Cosme* (0 h) — hero atenuado + aviso, escritorio y móvil (iframe 390 px). Solo en local (datos de prueba efímeros creados y borrados): alta de proyecto heredando, empresa de prueba (alta con tipología, edición, desactivación con confirmación, selectores sin inactivas con control positivo), alta rápida de categoría, cambio de rol con confirmación rechazada/aceptada. Comparación visual con el mock: selector "Tipología (área del mapa)" y colores de Empresas idénticos (Wowinx `#5E8C86`, Málaga `#6E8B72`, Legal Norte gris). **Todo dato de prueba revertido**: conteos de las 21 tablas + `auth.users` idénticos a los previos al Lote 4; T-014..T-017 como estaban; huella canónica `6c50f784` sin cambios.
- **Hallazgos NO corregidos (tareas separadas, sin tocar sin confirmación)**: (1) **Seguridad, RLS preexistente**: `perfil_update_admin` (001) deja a un admin_empresa cambiar el `rol` de cualquiera de su empresa, **incluido ella misma, a `admin_grupo`** con una llamada directa a la API (la UI lo bloquea, la BD no); reproducido con Marina y revertido al instante (Enrique = empleado, Marina = admin_empresa). Hace falta una guarda (trigger o `WITH CHECK` que impida a un no-admin_grupo tocar `rol`) y retest de las 3 identidades. (2) Orden no determinista de `resumen_dia`/`resumen_mes` (arriba). **(1) y (2) CERRADOS en la migración 021 (ver su sección).** (3) Aviso de hidratación en desarrollo por `data-theme` (script de tema previo a React): preexistente, solo dev.

### F6 Bloque B · Migración 021 — guarda de rol/empresa en `perfil` + desempate de `resumen_dia/mes` — hecho
- **Hallazgo cerrado (seguridad)**: `perfil_update_admin` (001) dejaba a un admin_empresa cambiar cualquier columna de los perfiles de su empresa, incluido el `rol` (el suyo a `admin_grupo`) por API directa. **Decisión del usuario (2026-09-19)**: cambiar el `rol` o la `empresa_id` de alguien es una operación estructural del grupo (la "puerta única") → ambas transiciones reservadas a `admin_grupo`; un admin_empresa sigue editando departamento y categoría de su gente, nada más.
- **Herramienta**: trigger `perfil_guardar_rol_empresa` `BEFORE UPDATE OF rol, empresa_id ON perfil` (RLS no puede comparar OLD/NEW: USING ve la fila vieja, WITH CHECK la nueva, nunca juntas). Compara con `IS DISTINCT FROM`, así que un UPDATE que reenvía los mismos valores (la ficha mandaba los cuatro campos) no se bloquea. Error: `42501` «Solo un admin del grupo puede cambiar el rol o la empresa de un usuario». Nota: para `empresa_id`, admin_empresa ya estaba frenado por el WITH CHECK (la fila nueva debe seguir en su empresa); la guarda lo hace explícito.
- **service_role, decidido y documentado**: los triggers SÍ aplican a service_role (RLS no) y con service_role `auth.uid()` es null → `es_admin_grupo()` sería null y bloquearía scripts legítimos. La guarda **exime** a los roles de BD que no son de usuario final (`current_user in ('authenticated','anon')` es lo único que se guarda): service_role/postgres son infraestructura de confianza (la service key no sale del servidor). Se usa `current_user`, no un claim del JWT, porque el trigger es SECURITY INVOKER y ese es el rol que fija PostgREST. Los importadores actuales no tocan rol/empresa en un UPDATE (entran por INSERT vía `handle_new_user`; el alta solo actualiza departamento/categoría) y `UPDATE OF` ni se dispara con ellos. INSERT no necesita guarda: `perfil.id` referencia `auth.users` y todo usuario ya tiene perfil.
- **UI alineada**: la ficha de usuario deja de renderizar los selects de Empresa y Rol a quien no es admin_grupo (antes estaban `disabled`), no envía esos campos y muestra la nota «La empresa y el rol de una persona solo los cambia un admin del grupo». La edición inline ya no ofrecía rol ni empresa a admin_empresa (`editable={esAdminGrupo}` para el rol; empresa no es editable inline). Ningún error de BD hace de validación.
- **Ride-along (independiente de la guarda)**: `resumen_dia` / `resumen_mes` con clave única al final del `ORDER BY` (`nombre, id` en `pendientes` y `ausentes`); cuerpos idénticos a los de 019 salvo esos tres `ORDER BY`. La huella cruda del RPC deja de depender del orden físico de `perfil`.
- **Reproducción ANTES/DESPUÉS con sesiones REALES (mismo script, revertido con service_role en `finally`)**: ANTES — Marina sube a Enrique a `admin_grupo` ✔ ACEPTADO, se sube a sí misma ✔ ACEPTADO, baja a Enrique a `responsable_proyecto` ✔ ACEPTADO (D, mover de empresa, ya lo frenaba el RLS). DESPUÉS de la 021 — esos tres y D **rechazados con el RAISE** y los roles no cambian. No se sobre-bloquea: Marina reenviando rol+empresa sin cambios ✔, cambiando departamento+categoría de Enrique ✔, solo departamento (inline) ✔; fuera de su empresa 0 filas; Andrés sobre sí mismo 0 filas; Cristian cambia rol y empresa de Enrique y lo devuelve ✔; **service_role** cambia rol+empresa ✔ (exención comprobada). Estado final == inicial en las dos pasadas.
- **Regresión**: huella canónica **`6c50f784` idéntica** antes y después de la 021. La cruda pasa de `d8d7dbcc` a **`fab8021d`** (por el desempate) y **ya es estable**: tras 26 UPDATE de sí mismos sobre `perfil` (mueven las tuplas físicas sin cambiar datos) sigue en `fab8021d…` (SHA-256 completo `fab8021dce04…`). Conteos de las 21 tablas + `auth.users` idénticos antes/después (incluidas las pruebas de importador). **Norma desde ahora**: cruda vigente `fab8021d`, canónica `6c50f784`; si la cruda se mueve sin cambio de datos, es un bug del desempate.
- **Ficha/Importador en el navegador, login explícito por identidad (local)**: *Marina* — «Editar datos» en la ficha de Enrique muestra solo Departamento y Categoría + nota; guardar dep/cat funciona (revertido); *Cristian* — ve Empresa y Rol; cambia empresa (→ Wowinx) y rol (→ Responsable) de Enrique desde la ficha con el aviso de confirmación y la BD lo acepta (revertido); **importador de usuarios** por la UI real (analiza 3 filas / 0 errores → importa 3) con empresa, departamento, categoría y rol correctos (Málaga + `responsable_proyecto` incluido) y borrado posterior; *Andrés* — `/admin/usuarios/<id>` lo expulsa a `/inicio`. Cookies `sb-*` borradas al terminar cada identidad.
- **Verificado en PRODUCCIÓN (redeploy Railway `SUCCESS`, commit `57b77ec`), login explícito por identidad** (había una sesión previa en el origen: borrada, no usada): *Marina* — la ficha de Enrique ofrece solo Departamento y Categoría + nota; guarda el departamento por la UI desplegada (rol y empresa intactos; revertido); *Cristian* — ve los cuatro campos; cambia empresa y rol de Enrique desde la ficha desplegada y la BD lo acepta (revertido); *Andrés* — `/admin/usuarios/<id>` lo expulsa a `/inicio`. La guarda es de BD (una sola base compartida por local y producción): el script ANTES/DESPUÉS ya corrió contra ella. Estado final: conteos de las 21 tablas + `auth.users` idénticos, roles como al inicio, cookies `sb-*` borradas, huellas `fab8021d` (cruda) / `6c50f784` (canónica).
- **Hallazgo nuevo, NO tocado (tarea separada)**: `invitarUsuario` (`src/app/admin/actions.ts:28`) impide a un admin_empresa invitar a `admin_grupo` o fuera de su empresa, pero SÍ le deja invitar con `rol = 'admin_empresa'` (alta de un par en su empresa): la UI no lo ofrece, el servidor lo permite (service_role, exento de la guarda por diseño). Leído en código, no reproducido. Propuesta: exigir `admin_grupo` para cualquier rol de admin en la validación del servidor.

### F6 Bloque B · Pulido — pendiente de feedback de demo, no iniciado
- ~~Bandeja de aprobación de imputaciones en admin móvil~~ — retirada: decisión de alcance cerrada en el Lote 4 (admin móvil vigila, no opera).
- QA móvil real, estados vacíos, accesibilidad (focus visible, aria de las hojas).
- Ejecución real del runbook de producción (proyecto Supabase nuevo, dominio propio).
- Despliegue base (Railway + dominio + variables) ya hecho en **F3.5**, contra el Supabase de demo — revisar antes de dar F6 por cerrada si el destino final de producción es el mismo proyecto Supabase/Railway o uno nuevo (credenciales, dominio propio, `additional_redirect_urls` de producción real en vez de las de demo/local).
- **Verificación de despliegue**: `/debug`, `/debug/movil` y `/debug/movil-admin` devuelven 404 en el build de producción — ya confirmado en F3.5 contra el propio despliegue de Railway; repetir si cambia el servicio o el dominio de destino.
- **Contraseña compartida (`Horas2026!`) es SOLO para la demo** (`scripts/set-passwords.mjs`) — sirve para poder enseñar la app desde un móvil real sin depender de magic links. En producción real: alta exclusivamente por invitación (`inviteUserByEmail`, sección Usuarios) + contraseña personal que cada usuario define y puede resetear por su cuenta — nunca una credencial compartida entre cuentas. El registro sigue cerrado en ambos flujos de login: `signInWithOtp` usa `shouldCreateUser:false` y `signInWithPassword` nunca crea usuarios (Supabase solo autentica contra cuentas ya existentes) — un email no invitado no puede entrar por ninguna de las dos vías.
- **Emails de demo en `@wowinx.com`, dominio ficticio que no recibe correo real** — consecuencia aceptada: el recovery por email queda muerto para las 11 cuentas de demo. Irrelevante hoy porque la demo usa la contraseña compartida conocida de arriba y `MODO_EMAIL=log` (nadie depende de recibir un correo real). Producción real sigue invitando usuarios reales por su propio email, sin este problema. **Actualización (Lote 1 pulido)**: el flujo "¿Olvidaste tu contraseña?" (`/login` → `resetPasswordForEmail` → `/auth/callback?next=/reset-password` → `/reset-password`) está construido completo y correcto end-to-end — reutiliza el mismo `/auth/callback` ya verificado con magic link — pero sigue inerte para las 11 cuentas `@wowinx.com` por el mismo motivo de arriba (dominio sin buzón real). En producción real, con emails reales, funciona sin cambios.

## 6. Seed de desarrollo

Empresas: Wowinx SL (B-11223344), Málaga CF SAD (A-99887766), Legal Norte SL (B-55667788). Departamentos: 3B3 (responsable Cristian Haro), Jurídico, Diseño. Categorías/subcategorías y proyectos: los de los mocks (Interno no refacturable). Usuarios (11): Cristian Haro (admin_grupo), Verónica Salguero, Andrés Fuentes, Leo Silva, Sara Martín, Ana Ruiz (Legal Norte), Cosme Hernandez, Daniel Ramírez, Marta Gil (Jurídico), Enrique Robles (empleado moroso de Málaga, usado en los mocks con vacaciones rechazadas y faltantes) y Marina Ortega (admin_empresa de Málaga CF SAD — persona aparte, no es Enrique). Tarifas: Desarrollo 60, Diseño 55, Abogados 95 (origen Legal Norte), Ana Ruiz 110. Imputaciones de julio y agosto completas (meses cerrados), septiembre parcial replicando los números de los mocks; 1 baja aprobada (Andrés 01–02/09), 2 vacaciones pendientes. Mapa del grupo (F6): 6 áreas y 22 elementos, literales de la constante `MAPA` de los mocks (Infraestructura, Tecnología, Deportes, Dinero, Cultura, Entretenimiento), con `empresa_id` resuelto contra las 3 empresas ya sembradas.

**Cuentas de la demo (13, leídas de `perfil`/`auth.users` el 2026-09-18)** — sin contraseñas: la compartida de la demo está en F6 Pulido y la de las cuentas personales la custodia el usuario.

| Nombre | Email | Rol | Empresa | Origen |
|---|---|---|---|---|
| Cristian Haro | cristian.haro@wowinx.com | admin_grupo | Wowinx SL | seed |
| Verónica Salguero | veronica.salguero@wowinx.com | empleado | Wowinx SL | seed |
| Andrés Fuentes | andres.fuentes@wowinx.com | empleado | Wowinx SL | seed |
| Leo Silva | leo.silva@wowinx.com | empleado | Wowinx SL | seed |
| Sara Martín | sara.martin@wowinx.com | empleado | Wowinx SL | seed |
| Ana Ruiz | ana.ruiz@wowinx.com | empleado | Legal Norte SL | seed |
| Cosme Hernandez | cosme.hernandez@wowinx.com | empleado | Wowinx SL | seed |
| Daniel Ramírez | daniel.ramirez@wowinx.com | empleado | Wowinx SL | seed |
| Marta Gil | marta.gil@wowinx.com | empleado | Legal Norte SL | seed |
| Enrique Robles | enrique.robles@wowinx.com | empleado | Málaga CF SAD | seed |
| Marina Ortega | marina.ortega@wowinx.com | admin_empresa | Málaga CF SAD | seed |
| Oliver Pérez | olcasan08@gmail.com | empleado | Wowinx SL | acceso real del propietario (invitación, 2026-09-08); **off-limits en pruebas** |
| **Haizea** | haizea@wowinx.com | **admin_grupo** | Wowinx SL | **cuenta legítima creada fuera de las sesiones de supervisión (2026-09-14)**; confirmado con el usuario |

## 7. Prompt inicial para Claude Code

```
Lee PLAN.md entero antes de escribir nada. Vamos a construir "Horas Grupo".

Reglas de trabajo:
- Los mocks de /mocks son la especificación de UI: replica layout, textos,
  estados y modo oscuro exactamente, extrayendo los tokens CSS a Tailwind.
- Toda la lógica de negocio está en /supabase/migrations: úsala (RPCs, vistas,
  triggers), no la dupliques en el cliente.
- TypeScript estricto, componentes de servidor por defecto, cliente solo donde
  hay interacción. Nada de librerías de UI ni de charts.

Tarea 1 (F1 del plan):
1. Inicializa Next.js 14 + Tailwind con los tokens del sistema Amable-gris
   (sección 3 del plan) y soporte de tema claro/oscuro por data-theme.
2. Conecta Supabase (@supabase/ssr), aplica las migraciones 001 y 002 y crea
   supabase/seed.sql según la sección 6.
3. Auth por invitación con magic link, middleware de sesión y hook usePerfil.
4. Página de smoke-test /debug que liste mis proyectos asignados (verifica RLS).
Al terminar: git add, resumen de decisiones y siguiente paso propuesto.
```

## 8. Estructura de repo propuesta

```
/mocks/                      ← los cuatro HTML (referencia, no se sirven)
/supabase/migrations/        ← 001, 002
/supabase/seed.sql
/src/app/(empleado)/...      ← inicio, imputar, calendario + hoja
/src/app/admin/...           ← layout sidebar + 11 secciones
/src/lib/supabase/           ← clientes server/browser, tipos generados
/src/components/ui/          ← Card, Kpi, Dot, Donut, Hbar, Sheet, Acal…
PLAN.md
```
